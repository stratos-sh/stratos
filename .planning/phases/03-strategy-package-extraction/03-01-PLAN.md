---
phase: 03-strategy-package-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/strategy/interface.go
  - internal/strategy/factory.go
  - internal/strategy/kubernetes/kubernetes.go
  - internal/strategy/kubernetes/drain.go
  - internal/strategy/kubernetes/events.go
  - internal/strategy/kubernetes/network.go
  - internal/strategy/kubernetes/capacity.go
  - internal/strategy/kubernetes/kubernetes_test.go
  - internal/strategy/kubernetes/events_test.go
  - internal/strategy/kubernetes/network_test.go
  - internal/strategy/githubactions/githubactions.go
  - internal/controller/reconciler.go
  - internal/controller/provider_cache.go
  - internal/controller/setup.go
  - internal/controller/pod_assignment_test.go
autonomous: true

must_haves:
  truths:
    - "strategy/ lives at internal/strategy/ as a top-level domain package"
    - "kubernetes implementation lives in internal/strategy/kubernetes/ sub-package"
    - "githubactions implementation lives in internal/strategy/githubactions/ sub-package"
    - "All 4 consumer files compile with new import paths"
    - "go build ./... passes with zero errors"
  artifacts:
    - path: "internal/strategy/interface.go"
      provides: "ScalingStrategy interface and shared types"
      contains: "type ScalingStrategy interface"
    - path: "internal/strategy/factory.go"
      provides: "Factory dispatching to sub-package constructors"
      contains: "internal/strategy/kubernetes"
    - path: "internal/strategy/kubernetes/kubernetes.go"
      provides: "KubernetesStrategy struct and constructor"
      contains: "package kubernetes"
    - path: "internal/strategy/githubactions/githubactions.go"
      provides: "GitHubActionsStrategy implementation"
      contains: "package githubactions"
  key_links:
    - from: "internal/strategy/factory.go"
      to: "internal/strategy/kubernetes/"
      via: "import and constructor call"
      pattern: "kubernetes\\.New"
    - from: "internal/controller/reconciler.go"
      to: "internal/strategy/"
      via: "import for ScalingStrategy type"
      pattern: "internal/strategy"
    - from: "internal/controller/setup.go"
      to: "internal/strategy/kubernetes/"
      via: "import for event handlers"
      pattern: "internal/strategy/kubernetes"
---

<objective>
Relocate the strategy package from internal/controller/strategy/ to internal/strategy/ and split implementations into sub-packages (kubernetes/, githubactions/).

Purpose: Makes strategy a first-class top-level package. Moves each implementation into its own sub-package following Go conventions (interface at root, implementations in sub-packages).
Output: Complete package relocation with all import paths updated and compilation passing.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-package-extraction/03-RESEARCH.md
@internal/controller/strategy/interface.go
@internal/controller/strategy/factory.go
@internal/controller/strategy/kubernetes.go
@internal/controller/strategy/githubactions.go
@internal/controller/strategy/kubernetes_drain.go
@internal/controller/strategy/kubernetes_events.go
@internal/controller/strategy/kubernetes_network.go
@internal/controller/strategy/kubernetes_capacity.go
@internal/controller/strategy/kubernetes_test.go
@internal/controller/strategy/kubernetes_events_test.go
@internal/controller/strategy/kubernetes_network_test.go
@internal/controller/reconciler.go
@internal/controller/provider_cache.go
@internal/controller/setup.go
@internal/controller/pod_assignment_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure and move files into sub-packages</name>
  <files>
    internal/strategy/interface.go
    internal/strategy/factory.go
    internal/strategy/kubernetes/kubernetes.go
    internal/strategy/kubernetes/drain.go
    internal/strategy/kubernetes/events.go
    internal/strategy/kubernetes/network.go
    internal/strategy/kubernetes/capacity.go
    internal/strategy/kubernetes/kubernetes_test.go
    internal/strategy/kubernetes/events_test.go
    internal/strategy/kubernetes/network_test.go
    internal/strategy/githubactions/githubactions.go
  </files>
  <action>
    Create the new directory structure and move files:

    1. Create directories:
       - `internal/strategy/`
       - `internal/strategy/kubernetes/`
       - `internal/strategy/githubactions/`

    2. Move root-level files to `internal/strategy/`:
       - `interface.go` -> `internal/strategy/interface.go` (keep package name `strategy`)
       - `factory.go` -> `internal/strategy/factory.go` (keep package name `strategy`)

    3. Move kubernetes files to `internal/strategy/kubernetes/`:
       - `kubernetes.go` -> `kubernetes/kubernetes.go`
       - `kubernetes_drain.go` -> `kubernetes/drain.go`
       - `kubernetes_events.go` -> `kubernetes/events.go`
       - `kubernetes_network.go` -> `kubernetes/network.go`
       - `kubernetes_capacity.go` -> `kubernetes/capacity.go`
       - `kubernetes_test.go` -> `kubernetes/kubernetes_test.go`
       - `kubernetes_events_test.go` -> `kubernetes/events_test.go`
       - `kubernetes_network_test.go` -> `kubernetes/network_test.go`

    4. Move githubactions file:
       - `githubactions.go` -> `githubactions/githubactions.go`

    5. Update package declarations:
       - All files in `kubernetes/` change from `package strategy` to `package kubernetes`
       - All files in `githubactions/` change from `package strategy` to `package githubactions`
       - `interface.go` and `factory.go` stay as `package strategy`

    6. Delete the old `internal/controller/strategy/` directory after all files are moved.

    Important: Do NOT change any function names, type names, or logic yet. Just move files and update package declarations. Import paths within the moved files stay as-is for now (they will break -- that's expected, fixed in Task 2).
  </action>
  <verify>
    - `ls internal/strategy/interface.go internal/strategy/factory.go` succeeds
    - `ls internal/strategy/kubernetes/kubernetes.go internal/strategy/kubernetes/drain.go` succeeds
    - `ls internal/strategy/githubactions/githubactions.go` succeeds
    - `ls internal/controller/strategy/` fails (directory removed)
    - All files in kubernetes/ have `package kubernetes` declaration
    - All files in githubactions/ have `package githubactions` declaration
  </verify>
  <done>All strategy files relocated to internal/strategy/ with correct sub-package structure. Old directory removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update all import paths and fix cross-package references</name>
  <files>
    internal/strategy/factory.go
    internal/strategy/interface.go
    internal/strategy/kubernetes/kubernetes.go
    internal/strategy/kubernetes/drain.go
    internal/strategy/kubernetes/events.go
    internal/strategy/kubernetes/network.go
    internal/strategy/kubernetes/capacity.go
    internal/strategy/kubernetes/kubernetes_test.go
    internal/strategy/kubernetes/events_test.go
    internal/strategy/kubernetes/network_test.go
    internal/strategy/githubactions/githubactions.go
    internal/controller/reconciler.go
    internal/controller/provider_cache.go
    internal/controller/setup.go
    internal/controller/pod_assignment_test.go
  </files>
  <action>
    Fix all import paths and cross-package references so the project compiles:

    **1. Update internal imports within moved files:**

    All files in `kubernetes/` and `githubactions/` that import `internal/controller/strategy` must update to import the parent package for interface types:
    - `"github.com/stratos-sh/stratos/internal/controller/strategy"` -> `"github.com/stratos-sh/stratos/internal/strategy"`
    - Since kubernetes/ files are now IN the kubernetes package, they access their own types directly (no import needed for same-package types)
    - The `kubernetes/kubernetes.go` file must import the parent `strategy` package to reference `strategy.ScalingStrategy`, `strategy.ScalingDemand`, `strategy.ScaleDownCandidate` types used in method signatures

    **2. Update factory.go imports:**

    Factory must import the sub-packages:
    ```go
    import (
        "github.com/stratos-sh/stratos/internal/strategy/kubernetes"
        "github.com/stratos-sh/stratos/internal/strategy/githubactions"
    )
    ```

    Update constructor calls:
    - `NewKubernetesStrategy(...)` -> `kubernetes.New(...)` (rename the constructor in kubernetes.go from `NewKubernetesStrategy` to `New`)
    - `NewGitHubActionsStrategy(...)` -> `githubactions.New(...)` (rename the constructor in githubactions.go from `NewGitHubActionsStrategy` to `New`)

    **3. Update consumer files (4 files):**

    All consumers change import path from:
    `"github.com/stratos-sh/stratos/internal/controller/strategy"` -> `"github.com/stratos-sh/stratos/internal/strategy"`

    Specific changes per consumer:

    a) `internal/controller/reconciler.go`:
       - Import path update only. Uses `strategy.ScalingStrategy`, `strategy.ScaleDownCandidate` -- these stay at the root, so no code changes needed.

    b) `internal/controller/provider_cache.go`:
       - Import path update for strategy root
       - Add import for `"github.com/stratos-sh/stratos/internal/strategy/kubernetes"` and `"github.com/stratos-sh/stratos/internal/strategy/githubactions"`
       - Update type references: `strategy.KubernetesStrategy` -> `kubernetes.Strategy` and `strategy.GitHubActionsStrategy` -> `githubactions.Strategy`
       - The struct types need renaming: In `kubernetes/kubernetes.go`, rename `KubernetesStrategy` to `Strategy`. In `githubactions/githubactions.go`, rename `GitHubActionsStrategy` to `Strategy`.
       - Update compile-time interface assertions accordingly.

    c) `internal/controller/setup.go`:
       - Add import for `"github.com/stratos-sh/stratos/internal/strategy/kubernetes"`
       - `strategy.KubernetesPodEventHandler` -> `kubernetes.PodEventHandler` (rename in kubernetes/events.go: `KubernetesPodEventHandler` -> `PodEventHandler`)
       - `strategy.KubernetesUnschedulablePodPredicate` -> `kubernetes.UnschedulablePodPredicate` (rename: `KubernetesUnschedulablePodPredicate` -> `UnschedulablePodPredicate`)

    d) `internal/controller/pod_assignment_test.go`:
       - Add import for `"github.com/stratos-sh/stratos/internal/strategy/kubernetes"`
       - `strategy.NewScaleCalculator` -> `kubernetes.NewScaleCalculator`

    **4. Rename exported symbols in sub-packages (drop redundant prefix):**

    In `kubernetes/` package:
    - `KubernetesStrategy` -> `Strategy`
    - `NewKubernetesStrategy` -> `New`
    - `KubernetesPodEventHandler` -> `PodEventHandler`
    - `KubernetesUnschedulablePodPredicate` -> `UnschedulablePodPredicate`

    In `githubactions/` package:
    - `GitHubActionsStrategy` -> `Strategy`
    - `NewGitHubActionsStrategy` -> `New`

    Update all references within each sub-package to use the new names (struct literals, method receivers, etc.).

    **5. Verify compilation:**

    Run `go build ./...` and fix any remaining import issues. The most likely issues will be:
    - Missing imports for types that moved between packages
    - Unexported symbols accessed from outside their package (should not happen since everything stays within kubernetes/ or githubactions/)
  </action>
  <verify>
    - `go build ./...` passes with zero errors
    - `grep -r "controller/strategy" internal/` returns zero matches (all old import paths removed)
    - `grep -r "package strategy" internal/strategy/kubernetes/` returns zero matches (all changed to package kubernetes)
    - `grep -r "package strategy" internal/strategy/githubactions/` returns zero matches (all changed to package githubactions)
    - `grep "type Strategy struct" internal/strategy/kubernetes/kubernetes.go` confirms KubernetesStrategy renamed to Strategy
    - `grep "func New(" internal/strategy/kubernetes/kubernetes.go` confirms NewKubernetesStrategy renamed to New
    - `grep "type Strategy struct" internal/strategy/githubactions/githubactions.go` confirms GitHubActionsStrategy renamed to Strategy
    - `grep "func New(" internal/strategy/githubactions/githubactions.go` confirms NewGitHubActionsStrategy renamed to New
  </verify>
  <done>All import paths updated, symbols renamed, compilation passes. No references to old internal/controller/strategy/ path remain.</done>
</task>

</tasks>

<verification>
1. `go build ./...` passes with zero errors
2. `ls internal/controller/strategy/` fails (old directory gone)
3. `ls internal/strategy/interface.go internal/strategy/factory.go` succeeds
4. `ls internal/strategy/kubernetes/kubernetes.go` succeeds
5. `ls internal/strategy/githubactions/githubactions.go` succeeds
6. `grep -r "controller/strategy" internal/` returns zero matches
7. `grep "package kubernetes" internal/strategy/kubernetes/kubernetes.go` returns a match
8. `grep "package githubactions" internal/strategy/githubactions/githubactions.go` returns a match
</verification>

<success_criteria>
- strategy/ lives at internal/strategy/ (not internal/controller/strategy/)
- kubernetes implementation is in internal/strategy/kubernetes/ sub-package
- githubactions implementation is in internal/strategy/githubactions/ sub-package
- Factory dispatches to sub-package constructors
- All 4 consumer files use new import paths
- go build ./... compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-package-extraction/03-01-SUMMARY.md`
</output>

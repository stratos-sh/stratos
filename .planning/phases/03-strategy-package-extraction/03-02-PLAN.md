---
phase: 03-strategy-package-extraction
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/strategy/kubernetes/kubernetes.go
  - internal/strategy/kubernetes/scaling.go
  - internal/strategy/kubernetes/pod_assignments.go
  - internal/strategy/kubernetes/maintenance.go
  - internal/strategy/kubernetes/readiness.go
  - internal/strategy/kubernetes/drain.go
  - internal/strategy/kubernetes/drain_eviction.go
  - internal/strategy/kubernetes/network.go
  - internal/strategy/kubernetes/kubernetes_test.go
  - internal/strategy/kubernetes/scaling_test.go
  - internal/strategy/kubernetes/maintenance_test.go
autonomous: true

must_haves:
  truths:
    - "kubernetes.go (formerly 910 lines) is decomposed into focused files by concern"
    - "No single file in internal/strategy/kubernetes/ exceeds 300 lines"
    - "drain.go (formerly 358 lines) is split into drain.go + drain_eviction.go"
    - "networkReadinessChecker is instantiated once per Strategy construction and reused"
    - "go build ./... and go test ./internal/strategy/... both pass"
  artifacts:
    - path: "internal/strategy/kubernetes/kubernetes.go"
      provides: "Strategy struct, constructor, DrainAndStop wrapper, node helpers"
      contains: "func New("
    - path: "internal/strategy/kubernetes/scaling.go"
      provides: "CheckDemand, OnScaleUp, FindScaleDownCandidates"
      contains: "func.*CheckDemand"
    - path: "internal/strategy/kubernetes/pod_assignments.go"
      provides: "Pod assignment filtering, creation, estimation, cleanup"
      contains: "func.*filterAssignedPods"
    - path: "internal/strategy/kubernetes/maintenance.go"
      provides: "RunMaintenance and sub-tasks"
      contains: "func.*RunMaintenance"
    - path: "internal/strategy/kubernetes/readiness.go"
      provides: "IsReady, PrepareForRunning, PrepareForStandby, taint processing"
      contains: "func.*IsReady"
    - path: "internal/strategy/kubernetes/drain.go"
      provides: "drainHelper struct, core drain methods"
      contains: "type drainHelper struct"
    - path: "internal/strategy/kubernetes/drain_eviction.go"
      provides: "Pod filtering, eviction, waiting logic"
      contains: "func.*evictPod"
  key_links:
    - from: "internal/strategy/kubernetes/scaling.go"
      to: "internal/strategy/kubernetes/pod_assignments.go"
      via: "scaling calls pod assignment functions"
      pattern: "filterAssignedPods|createPodAssignments"
    - from: "internal/strategy/kubernetes/readiness.go"
      to: "internal/strategy/kubernetes/network.go"
      via: "readiness uses networkReadinessChecker field"
      pattern: "s\\.networkChecker"
    - from: "internal/strategy/kubernetes/kubernetes.go"
      to: "internal/strategy/kubernetes/drain.go"
      via: "DrainAndStop calls drainHelper"
      pattern: "drainHelper"
---

<objective>
Decompose the 910-line kubernetes.go into focused files by concern, split the 358-line drain.go to meet the 300-line cap, and make networkReadinessChecker a reused field.

Purpose: Every file in the kubernetes/ sub-package has a clear single concern and stays under 300 lines. This is the core decomposition that makes the strategy code navigable.
Output: 7 focused source files replacing the monolithic kubernetes.go, plus drain split into 2 files. All under 300 lines.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-package-extraction/03-RESEARCH.md
@.planning/phases/03-strategy-package-extraction/03-01-SUMMARY.md
@internal/strategy/kubernetes/kubernetes.go
@internal/strategy/kubernetes/drain.go
@internal/strategy/kubernetes/network.go
@internal/strategy/kubernetes/kubernetes_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose kubernetes.go into focused files and refactor networkReadinessChecker</name>
  <files>
    internal/strategy/kubernetes/kubernetes.go
    internal/strategy/kubernetes/scaling.go
    internal/strategy/kubernetes/pod_assignments.go
    internal/strategy/kubernetes/maintenance.go
    internal/strategy/kubernetes/readiness.go
  </files>
  <action>
    Split the 910-line kubernetes.go into 5 focused files. After splitting, kubernetes.go should be ~140 lines (struct, constructor, DrainAndStop, helpers). All new files stay in `package kubernetes`.

    **File 1: kubernetes.go (keep -- trim to ~140 lines)**
    Keep ONLY:
    - Package declaration, imports
    - `Strategy` struct definition (add `networkChecker *networkReadinessChecker` field)
    - `New()` constructor (initialize `networkChecker` field: `networkChecker: newNetworkReadinessChecker(c, cniPodSelector)`)
    - `DrainAndStop()` method (thin wrapper ~39 lines -- calls drainHelper then stops instance)
    - `getNodesForPool()`, `getRunningNodes()` helper methods (~36 lines)
    - `getNodeClass()` method
    - `getUnschedulablePods()` method
    Remove everything else (it goes into the new files below).

    **File 2: scaling.go (new -- ~215 lines)**
    Extract:
    - `CheckDemand()` method -- demand detection logic
    - `OnScaleUp()` method -- post-scale-up pod assignment
    - `FindScaleDownCandidates()` method -- scale-down candidate selection
    - `countStartingNodes()` helper
    - Any imports these functions need

    **File 3: pod_assignments.go (new -- ~180 lines)**
    Extract:
    - `filterAssignedPods()` method
    - `createPodAssignments()` method
    - `estimatePodsPerNode()` method
    - `cleanupPodAssignments()` method
    - Any imports these functions need

    **File 4: maintenance.go (new -- ~157 lines)**
    Extract:
    - `RunMaintenance()` method -- the coordinator that calls sub-tasks
    - `ensureTemplateLabels()` method
    - `clearStaleScaleUpAnnotations()` method
    - `removeScaleUpAnnotation()` helper
    - Any imports these functions need

    **File 5: readiness.go (new -- ~202 lines)**
    Extract:
    - `IsReady()` method -- change to use `s.networkChecker` instead of calling `newNetworkReadinessChecker`
    - `PrepareForRunning()` method
    - `PrepareForStandby()` method
    - `processStartupTaints()` method -- change to use `s.networkChecker`
    - `getNodeStartTime()` helper
    - `forceRemoveNetworkReadinessTaint()` method
    - `removeNetworkReadinessTaintWhenReady()` method -- change to use `s.networkChecker`
    - Any imports these functions need

    **networkReadinessChecker refactoring:**
    In readiness.go, replace all calls to `newNetworkReadinessChecker(s.client, s.cniPodSelector)` with `s.networkChecker`. This applies to:
    - `IsReady()` (approx line 299 of current kubernetes.go)
    - `removeNetworkReadinessTaintWhenReady()` (approx line 784 of current kubernetes.go)
    - Any other call sites within readiness functions

    **Important:**
    - All files use `package kubernetes`
    - All methods stay as methods on `*Strategy` (the receiver does not change)
    - No function logic changes -- pure file-level decomposition
    - The only behavioral change is networkReadinessChecker instantiation (moved to constructor, used via field)
  </action>
  <verify>
    - `go build ./...` passes
    - `wc -l internal/strategy/kubernetes/kubernetes.go` shows <= 300 lines
    - `wc -l internal/strategy/kubernetes/scaling.go` shows <= 300 lines
    - `wc -l internal/strategy/kubernetes/pod_assignments.go` shows <= 300 lines
    - `wc -l internal/strategy/kubernetes/maintenance.go` shows <= 300 lines
    - `wc -l internal/strategy/kubernetes/readiness.go` shows <= 300 lines
    - `grep "networkChecker" internal/strategy/kubernetes/kubernetes.go` shows field in struct
    - `grep "s.networkChecker" internal/strategy/kubernetes/readiness.go` shows usage (not newNetworkReadinessChecker)
    - `grep "func.*CheckDemand" internal/strategy/kubernetes/scaling.go` confirms CheckDemand is in scaling.go
    - `grep "func.*filterAssignedPods" internal/strategy/kubernetes/pod_assignments.go` confirms filterAssignedPods is in pod_assignments.go
    - `grep "func.*RunMaintenance" internal/strategy/kubernetes/maintenance.go` confirms RunMaintenance is in maintenance.go
    - `grep "func.*IsReady" internal/strategy/kubernetes/readiness.go` confirms IsReady is in readiness.go
  </verify>
  <done>kubernetes.go decomposed into 5 focused files, all under 300 lines. networkReadinessChecker instantiated once in constructor and reused via field.</done>
</task>

<task type="auto">
  <name>Task 2: Split drain.go and decompose test files</name>
  <files>
    internal/strategy/kubernetes/drain.go
    internal/strategy/kubernetes/drain_eviction.go
    internal/strategy/kubernetes/kubernetes_test.go
    internal/strategy/kubernetes/scaling_test.go
    internal/strategy/kubernetes/maintenance_test.go
  </files>
  <action>
    **Part A: Split drain.go (358 lines) into two files under 300 lines each.**

    Split at the natural boundary identified in research:

    `drain.go` (~177 lines) -- keep:
    - Package declaration, imports
    - `drainHelper` struct definition
    - `newDrainHelper()` constructor
    - `CordonNode()` method
    - `UncordonNode()` method
    - `DrainNode()` method (the orchestrator that calls eviction)

    `drain_eviction.go` (~181 lines) -- extract:
    - Package declaration, imports
    - `getPodsOnNode()` method
    - `filterPodsToEvict()` method
    - `hasLocalStorage()` function
    - `evictPod()` method
    - `waitForPodsDeletion()` method
    - `waitForPodDeletion()` method
    - `isNodeEmpty()` method
    - `isDaemonSetPod()` function

    **Part B: Split kubernetes_test.go (510 lines) into focused test files.**

    Examine the test functions in kubernetes_test.go and split by concern to match the new source files:

    `kubernetes_test.go` (~keep only tests for constructor/helpers/DrainAndStop -- whatever remains after extraction)

    `scaling_test.go` (new) -- extract tests for:
    - `CheckDemand` tests
    - `FindScaleDownCandidates` tests
    - `countStartingNodes` tests
    - Any scale-related helper tests

    `maintenance_test.go` (new) -- extract tests for:
    - `ensureTemplateLabels` tests
    - `processStartupTaints` tests (if in kubernetes_test.go)
    - Maintenance-related tests

    Note: events_test.go and network_test.go already exist as separate files from Plan 01 relocation. They should already be correctly placed. Only split kubernetes_test.go.

    **Important:**
    - All test files use `package kubernetes` (NOT `package kubernetes_test`) to maintain access to unexported symbols
    - If test helper functions (setup helpers, fixture builders, etc.) are used by multiple test files, extract them into `helpers_test.go` so they are accessible to all test files in the package
    - If helpers are only used by one test file, keep them in that test file
    - Target: kubernetes_test.go should be under 300 lines after extraction. If it would exceed 300 lines, extract more tests to scaling_test.go or maintenance_test.go as appropriate
  </action>
  <verify>
    - `wc -l internal/strategy/kubernetes/drain.go` shows <= 300 lines
    - `wc -l internal/strategy/kubernetes/drain_eviction.go` shows <= 300 lines
    - `wc -l internal/strategy/kubernetes/kubernetes_test.go` shows <= 300 lines (reduced from 510)
    - `wc -l internal/strategy/kubernetes/*.go | sort -n` -- verify ALL files (including test files) are under 300 lines
    - `go build ./...` passes
    - `go test ./internal/strategy/...` passes (or at minimum compiles -- break-and-fix approach allows test failures during restructure)
  </verify>
  <done>drain.go split into drain.go + drain_eviction.go (both under 300 lines). kubernetes_test.go split into focused test files matching source structure. All files in kubernetes/ under 300 lines.</done>
</task>

</tasks>

<verification>
1. `go build ./...` passes with zero errors
2. `wc -l internal/strategy/kubernetes/*.go | sort -n` -- every file under 300 lines
3. `ls internal/strategy/kubernetes/` shows: kubernetes.go, scaling.go, pod_assignments.go, maintenance.go, readiness.go, drain.go, drain_eviction.go, network.go, capacity.go, events.go, plus test files
4. `grep "networkChecker.*networkReadinessChecker" internal/strategy/kubernetes/kubernetes.go` confirms checker is a struct field
5. `grep "newNetworkReadinessChecker" internal/strategy/kubernetes/readiness.go` returns zero matches (replaced with s.networkChecker)
6. `go test ./internal/strategy/...` compiles (tests may fail per break-and-fix policy, but must compile)
</verification>

<success_criteria>
- kubernetes.go (910 lines) replaced by 5+ focused files, each under 300 lines
- drain.go (358 lines) split into drain.go + drain_eviction.go, each under 300 lines
- networkReadinessChecker instantiated once in constructor, reused via struct field
- Test files split to match source file structure
- All files in internal/strategy/kubernetes/ under 300 lines
- go build ./... passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-package-extraction/03-02-SUMMARY.md`
</output>

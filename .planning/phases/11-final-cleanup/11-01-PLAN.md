---
phase: 11-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/nodepool/reconciler.go
  - internal/scaling/drain.go
  - internal/scaling/kubernetes.go
  - internal/controller/nodepool/nodestate/doc.go
  - internal/metrics/doc.go
  - go.mod
  - go.sum
autonomous: true

must_haves:
  truths:
    - "RBAC markers no longer grant Secrets access"
    - "drainHelper struct has no unused recorder field"
    - "Doc comments reference scaling package, not deleted strategy packages"
    - "go.sum contains no GitHub API client dependencies"
    - "All unit tests pass (make test)"
    - "All integration tests pass (make test-integration)"
    - "Linter passes (make lint)"
  artifacts:
    - path: "internal/controller/nodepool/reconciler.go"
      provides: "RBAC markers without secrets access"
      contains: "no secrets RBAC marker line"
    - path: "internal/scaling/drain.go"
      provides: "drainHelper without dead recorder field"
      contains: "no recorder field or events import"
    - path: "internal/scaling/kubernetes.go"
      provides: "Updated newDrainHelper call site"
      contains: "newDrainHelper(s.client, drainCfg)"
  key_links:
    - from: "internal/scaling/kubernetes.go"
      to: "internal/scaling/drain.go"
      via: "newDrainHelper call"
      pattern: "newDrainHelper\\(s\\.client, drainCfg\\)"
---

<objective>
Remove all residual references to the deleted strategy/GitHub packages (RBAC marker, dead code, stale doc comments), tidy dependencies, and verify the full test/lint/build suites pass clean.

Purpose: Final phase of v1.1 Simplify Scaling milestone -- ensures the codebase is fully cleaned up after the strategy package removal in phases 7-10.
Output: Clean codebase with all verification suites passing.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-final-cleanup/11-RESEARCH.md
@internal/controller/nodepool/reconciler.go
@internal/scaling/drain.go
@internal/scaling/kubernetes.go
@internal/controller/nodepool/nodestate/doc.go
@internal/metrics/doc.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove residual code references and dead code</name>
  <files>
    internal/controller/nodepool/reconciler.go
    internal/scaling/drain.go
    internal/scaling/kubernetes.go
    internal/controller/nodepool/nodestate/doc.go
    internal/metrics/doc.go
  </files>
  <action>
    Five targeted edits:

    1. **Remove RBAC secrets marker** in `reconciler.go`:
       Delete the line `// +kubebuilder:rbac:groups="",resources=secrets,verbs=get;list;watch` (around line 84).
       Do NOT touch any other RBAC markers -- all others are actively used.

    2. **Remove dead recorder field from drainHelper** in `drain.go`:
       - Remove the `recorder events.EventRecorder` field from the `drainHelper` struct
       - Remove the `recorder events.EventRecorder` parameter from the `newDrainHelper` function signature
       - Remove the `recorder: recorder,` assignment inside `newDrainHelper`
       - Remove the `"k8s.io/client-go/tools/events"` import (becomes unused after field removal)
       IMPORTANT: Do NOT touch the `Strategy` struct or its `recorder` field in `kubernetes.go` -- that recorder IS actively used in `readiness.go`.

    3. **Update newDrainHelper call site** in `kubernetes.go`:
       Change `newDrainHelper(s.client, s.recorder, drainCfg)` to `newDrainHelper(s.client, drainCfg)` (around line 77).
       Do NOT modify the `Strategy` struct, `New()` constructor, or any other code in this file.

    4. **Update stale doc comment** in `nodestate/doc.go`:
       Change "imported by lifecycle, nodepool, strategy/kubernetes, and strategy/githubactions." to "imported by lifecycle, nodepool, and scaling."

    5. **Update stale doc comment** in `metrics/doc.go`:
       Change "the aws, lifecycle, nodepool, and strategy/kubernetes packages." to "the aws, lifecycle, nodepool, and scaling packages."
  </action>
  <verify>
    `go build ./...` succeeds with no compilation errors.
    `grep -r 'strategy/' internal/controller/nodepool/nodestate/doc.go internal/metrics/doc.go` returns no matches.
    `grep 'resources=secrets' internal/controller/nodepool/reconciler.go` returns no matches.
    `grep -E 'recorder.*EventRecorder|newDrainHelper.*recorder' internal/scaling/drain.go` returns no matches.
    `grep 'func newDrainHelper(c client.Client, config \*drainConfig)' internal/scaling/drain.go` matches (confirms correct 2-parameter signature).
  </verify>
  <done>
    RBAC secrets marker removed. drainHelper has no dead recorder field. Doc comments reference scaling package. Code compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tidy dependencies and run full verification suite</name>
  <files>
    go.mod
    go.sum
  </files>
  <action>
    1. Run `go mod tidy` to clean any unused dependencies. This is expected to be a no-op or minimal change (GitHub API deps were already removed in prior phases), but serves as a safety net.

    2. Verify no GitHub API client dependencies remain:
       `grep -i 'go-github\|ghinstallation\|google/go-github' go.sum` should return no matches.

    3. Verify depguard is clean (already met, just confirm):
       `grep 'strategy/' .golangci.yml` should return no matches.

    4. Run full verification suite in order:
       - `go build ./...` (compilation check)
       - `make test` (all unit tests)
       - `make test-integration` (all integration tests)
       - `make lint` (linter clean)

    All four commands must exit 0.
  </action>
  <verify>
    `make test` exits 0.
    `make test-integration` exits 0.
    `make lint` exits 0.
  </verify>
  <done>
    Dependencies tidied. No GitHub API deps in go.sum. All unit tests pass. All integration tests pass. Linter passes clean. Phase 11 and v1.1 milestone complete.
  </done>
</task>

</tasks>

<verification>
All six phase success criteria verified:
1. `grep 'resources=secrets' internal/controller/nodepool/reconciler.go` returns no matches
2. `grep -i 'go-github\|ghinstallation' go.sum` returns no matches
3. `grep 'strategy/' .golangci.yml` returns no matches
4. `make test` exits 0
5. `make test-integration` exits 0
6. `make lint` exits 0
</verification>

<success_criteria>
- RBAC markers grant no Secrets access
- go.sum contains no GitHub API client dependencies
- .golangci.yml has no strategy/ references
- `make test` passes
- `make test-integration` passes
- `make lint` passes
- drainHelper has no dead recorder field
- Doc comments reference scaling, not strategy/kubernetes or strategy/githubactions
</success_criteria>

<output>
After completion, create `.planning/phases/11-final-cleanup/11-01-SUMMARY.md`
</output>

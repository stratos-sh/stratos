---
phase: 04-controller-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/cluster_config.go
  - internal/config/cluster_config_test.go
  - internal/controller/nodepool/reconciler.go
  - internal/controller/nodepool/setup.go
  - internal/controller/nodepool/cloud_sync.go
  - internal/controller/nodepool/node_queries.go
  - internal/controller/nodepool/nodepool_status.go
  - internal/controller/nodepool/nodepool_validation.go
  - internal/controller/nodepool/pool_maintenance.go
  - internal/controller/nodepool/provider_cache.go
  - internal/controller/nodepool/pod_assignment_test.go
  - internal/controller/nodepool/lifecycle/manager.go
  - internal/controller/nodepool/lifecycle/node_launch.go
  - internal/controller/nodepool/lifecycle/node_startstop.go
  - internal/controller/nodepool/lifecycle/node_sync.go
  - internal/controller/nodepool/lifecycle/warmup_adoption.go
  - internal/controller/nodepool/lifecycle/warmup_handlers.go
  - internal/controller/nodepool/lifecycle/warmup_monitor.go
  - internal/controller/nodepool/lifecycle/warmup_test.go
  - internal/controller/nodepool/nodestate/nodestate.go
  - internal/controller/nodepool/nodestate/nodestate_test.go
autonomous: true

must_haves:
  truths:
    - "ClusterConfig type lives in internal/config/ alongside the existing Config type"
    - "All NodePool reconciliation logic lives in internal/controller/nodepool/ with package name 'nodepool'"
    - "lifecycle/ and nodestate/ are sub-packages of nodepool/ reflecting their sole consumer relationship"
    - "go build ./internal/controller/nodepool/... compiles cleanly (full build deferred to Plan 02)"
  artifacts:
    - path: "internal/config/cluster_config.go"
      provides: "ClusterConfig type, Validate(), DetectKubernetesVersion()"
      contains: "package config"
    - path: "internal/controller/nodepool/reconciler.go"
      provides: "Reconciler type (renamed from NodePoolReconciler), Reconcile(), reconcileNodePool()"
      contains: "package nodepool"
    - path: "internal/controller/nodepool/setup.go"
      provides: "SetupWithManager() and event handlers"
      contains: "package nodepool"
    - path: "internal/controller/nodepool/provider_cache.go"
      provides: "ensureCloudProvider, getOrCreateStrategy, InjectCloudProvider"
      contains: "package nodepool"
  key_links:
    - from: "internal/controller/nodepool/reconciler.go"
      to: "internal/config"
      via: "ClusterConfig field import"
      pattern: "config\\.ClusterConfig"
    - from: "internal/controller/nodepool/provider_cache.go"
      to: "internal/controller/nodepool/lifecycle"
      via: "lifecycle.NewManager import"
      pattern: "lifecycle\\.NewManager"
    - from: "internal/controller/nodepool/lifecycle/"
      to: "internal/controller/nodepool/nodestate/"
      via: "nodestate constants import"
      pattern: "nodestate\\."
---

<objective>
Move cluster_config.go to internal/config/ and create the nodepool/ sub-package by relocating all NodePool reconciliation files (8 source files, 1 test file) plus lifecycle/ and nodestate/ sub-packages under nodepool/.

Purpose: This is the foundational step of the controller split. By moving all NodePool-specific code into its own package, we establish the per-CRD package boundary that the nodeclass/ package (Plan 02) and the aggregator setup.go depend on.

Output: A fully compiling internal/controller/nodepool/ package containing the Reconciler type, all delegate files, lifecycle/ and nodestate/ sub-packages, and a refactored internal/config/ package with ClusterConfig.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-controller-split/04-CONTEXT.md
@.planning/phases/04-controller-split/04-RESEARCH.md
@internal/controller/reconciler.go
@internal/controller/setup.go
@internal/controller/cluster_config.go
@internal/controller/cluster_config_test.go
@internal/controller/provider_cache.go
@internal/controller/cloud_sync.go
@internal/controller/node_queries.go
@internal/controller/nodepool_status.go
@internal/controller/nodepool_validation.go
@internal/controller/pool_maintenance.go
@internal/controller/pod_assignment_test.go
@internal/config/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move cluster_config.go to internal/config/</name>
  <files>
    internal/config/cluster_config.go
    internal/config/cluster_config_test.go
  </files>
  <action>
    Move `internal/controller/cluster_config.go` to `internal/config/cluster_config.go`:
    1. Copy the file content, change `package controller` to `package config`
    2. Remove imports that referenced controller-internal types (there are none -- cluster_config.go only imports standard library and k8s discovery)
    3. The types ClusterConfig, DetectKubernetesVersion(), parseKubernetesVersion(), Validate(), and all validation helpers move as-is

    Move `internal/controller/cluster_config_test.go` to `internal/config/cluster_config_test.go`:
    1. Change `package controller` to `package config`
    2. Test functions reference ClusterConfig directly (no package prefix needed since same package)
    3. Test for parseKubernetesVersion uses `Test_` prefix -- keep this convention

    Delete the original files from internal/controller/:
    - `internal/controller/cluster_config.go`
    - `internal/controller/cluster_config_test.go`

    Do NOT update consumers yet (main.go, reconciler.go, suite_test.go) -- those updates happen in Task 2 and Plan 02.
  </action>
  <verify>
    Run `go build ./internal/config/...` to verify the config package compiles.
    Run `go test ./internal/config/...` to verify cluster_config tests pass in their new home.
  </verify>
  <done>
    ClusterConfig type lives in internal/config/ package. Tests pass. Original files deleted from controller/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create nodepool/ package and move all NodePool source files</name>
  <files>
    internal/controller/nodepool/reconciler.go
    internal/controller/nodepool/setup.go
    internal/controller/nodepool/cloud_sync.go
    internal/controller/nodepool/node_queries.go
    internal/controller/nodepool/nodepool_status.go
    internal/controller/nodepool/nodepool_validation.go
    internal/controller/nodepool/pool_maintenance.go
    internal/controller/nodepool/provider_cache.go
    internal/controller/nodepool/pod_assignment_test.go
  </files>
  <action>
    Create `internal/controller/nodepool/` directory and move all NodePool-specific source files into it.

    For EACH file being moved, apply these transformations:
    1. Change `package controller` to `package nodepool`
    2. Rename `NodePoolReconciler` to `Reconciler` throughout (Go convention: type name should not stutter with package name)
    3. Update import paths:
       - `"github.com/stratos-sh/stratos/internal/controller/nodestate"` becomes `"github.com/stratos-sh/stratos/internal/controller/nodepool/nodestate"`
       - `"github.com/stratos-sh/stratos/internal/controller/lifecycle"` becomes `"github.com/stratos-sh/stratos/internal/controller/nodepool/lifecycle"`
       - Add `"github.com/stratos-sh/stratos/internal/config"` where ClusterConfig is referenced (reconciler.go, provider_cache.go)
    4. In reconciler.go: Change `ClusterConfig *ClusterConfig` field to `ClusterConfig *config.ClusterConfig` (importing from internal/config)
    5. In provider_cache.go: Update the aws.ClusterConfig conversion to reference `r.ClusterConfig` fields (same code, just the type import changes)
    6. In setup.go: The `kubernetes.PodEventHandler` import stays as `"github.com/stratos-sh/stratos/internal/strategy/kubernetes"` (no change)

    Also keep `nodeclass_lifecycle.go` and `nodeclass_lifecycle_test.go` in controller/ root for now -- they will move to the nodeclass package in Plan 02.

    Delete the original source files from internal/controller/ (NOT nodeclass_lifecycle.go, NOT nodeclass_lifecycle_test.go).

    IMPORTANT: nodeclass_lifecycle.go references NodePoolReconciler. Since it stays in controller/ root temporarily, it will NOT compile until Plan 02 extracts it into its own package. This is expected -- the "break-and-fix" approach is established project convention.
  </action>
  <verify>
    Run `go build ./internal/controller/nodepool/...` to verify the nodepool package compiles in isolation.
    Run `go vet ./internal/controller/nodepool/...` to check for issues.
    Verify the file count: `ls internal/controller/nodepool/*.go | wc -l` should show 8 source files + 1 test file = 9 files.
  </verify>
  <done>
    All 8 NodePool source files and 1 test file live in internal/controller/nodepool/. Package declaration is `nodepool`. Reconciler type is named `Reconciler` (not `NodePoolReconciler`). Import paths reference nodepool/lifecycle and nodepool/nodestate. The nodepool package compiles independently.
  </done>
</task>

<task type="auto">
  <name>Task 3: Move lifecycle/ and nodestate/ under nodepool/</name>
  <files>
    internal/controller/nodepool/lifecycle/manager.go
    internal/controller/nodepool/lifecycle/node_launch.go
    internal/controller/nodepool/lifecycle/node_startstop.go
    internal/controller/nodepool/lifecycle/node_sync.go
    internal/controller/nodepool/lifecycle/warmup_adoption.go
    internal/controller/nodepool/lifecycle/warmup_handlers.go
    internal/controller/nodepool/lifecycle/warmup_monitor.go
    internal/controller/nodepool/lifecycle/warmup_test.go
    internal/controller/nodepool/nodestate/nodestate.go
    internal/controller/nodepool/nodestate/nodestate_test.go
  </files>
  <action>
    Move the existing `internal/controller/lifecycle/` directory to `internal/controller/nodepool/lifecycle/`:
    1. For each .go file: NO package declaration change needed (package name stays `lifecycle`)
    2. Update internal import paths: `"github.com/stratos-sh/stratos/internal/controller/nodestate"` becomes `"github.com/stratos-sh/stratos/internal/controller/nodepool/nodestate"` in every lifecycle/*.go file that imports nodestate
    3. Delete the original `internal/controller/lifecycle/` directory

    Move the existing `internal/controller/nodestate/` directory to `internal/controller/nodepool/nodestate/`:
    1. For each .go file: NO package declaration change needed (package name stays `nodestate`)
    2. nodestate/ has no internal imports -- it is a pure leaf package. No import path changes needed inside it.
    3. Delete the original `internal/controller/nodestate/` directory

    Update ALL consumers of the old import paths across the codebase:
    - `internal/strategy/kubernetes/*.go` files that import `controller/nodestate` must update to `controller/nodepool/nodestate`
    - `internal/strategy/kubernetes/*.go` files that import `controller/lifecycle` must update to `controller/nodepool/lifecycle`
    - `tests/integration/*.go` files that import `controller/nodestate` must update to `controller/nodepool/nodestate`
    - `nodeclass_lifecycle.go` in controller/ root does NOT import nodestate or lifecycle, so no update needed there

    Verify no stale references remain by searching: `grep -r "controller/nodestate" --include="*.go"` should only show paths containing `nodepool/nodestate`. Similarly for `controller/lifecycle`.
  </action>
  <verify>
    Run `go build ./internal/controller/nodepool/...` to verify nodepool and its sub-packages compile.
    Run `go test ./internal/controller/nodepool/nodestate/...` to verify nodestate tests pass.
    Run `grep -r '"github.com/stratos-sh/stratos/internal/controller/nodestate"' --include="*.go"` should return zero results (all should be `controller/nodepool/nodestate` now).
    Run `grep -r '"github.com/stratos-sh/stratos/internal/controller/lifecycle"' --include="*.go"` should return zero results.
  </verify>
  <done>
    lifecycle/ and nodestate/ live under internal/controller/nodepool/. All import paths updated across the codebase. No stale import paths remain. The nodepool package with its sub-packages compiles cleanly.
  </done>
</task>

</tasks>

<verification>
After all three tasks:
1. `go build ./internal/config/...` compiles (ClusterConfig in new home)
2. `go build ./internal/controller/nodepool/...` compiles (all NodePool files + lifecycle + nodestate)
3. `go test ./internal/config/...` passes (cluster_config tests)
4. `go test ./internal/controller/nodepool/nodestate/...` passes (nodestate tests)
5. `grep -r '"github.com/stratos-sh/stratos/internal/controller/nodestate"' --include="*.go"` returns nothing
6. `grep -r '"github.com/stratos-sh/stratos/internal/controller/lifecycle"' --include="*.go"` returns nothing
7. Only `nodeclass_lifecycle.go`, `nodeclass_lifecycle_test.go` remain in controller/ root (these move in Plan 02)

Note: `go build ./...` will NOT pass yet because:
- main.go still references `controller.ClusterConfig` and `controller.NodePoolReconciler`
- nodeclass_lifecycle.go still references `NodePoolReconciler` which no longer exists in controller/
- Integration tests still import `controller.NodePoolReconciler`
These are all fixed in Plan 02.
</verification>

<success_criteria>
- internal/controller/nodepool/ package exists with 8 source files and 1 test file
- internal/controller/nodepool/lifecycle/ has 7 source files and 1 test file
- internal/controller/nodepool/nodestate/ has 1 source file and 1 test file
- internal/config/cluster_config.go exists with ClusterConfig type
- Reconciler type in nodepool/ is named `Reconciler` (not `NodePoolReconciler`)
- All import paths reference nodepool/lifecycle and nodepool/nodestate (no stale paths)
- go build ./internal/controller/nodepool/... compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-controller-split/04-01-SUMMARY.md`
</output>

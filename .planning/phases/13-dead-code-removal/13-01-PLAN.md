---
phase: 13-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/scaling/drain.go]
autonomous: true

must_haves:
  truths:
    - "UncordonNode method does not exist anywhere in the codebase"
    - "CordonNode method still exists and has at least one caller"
    - "go build and go test pass after deletion"
  artifacts:
    - path: "internal/scaling/drain.go"
      provides: "drain helpers (CordonNode, DrainNode) without dead UncordonNode"
      contains: "func (d *drainHelper) CordonNode"
      not_contains: "UncordonNode"
  key_links:
    - from: "internal/scaling/drain.go"
      to: "CordonNode caller in DrainNode"
      via: "method call on line 127"
      pattern: "d\\.CordonNode"
---

<objective>
Remove the dead UncordonNode method from internal/scaling/drain.go.

Purpose: Eliminate dead code identified by the deadcode tool -- UncordonNode has zero callers.
Output: drain.go with UncordonNode removed, CordonNode preserved, build and tests green.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/scaling/drain.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete UncordonNode and verify</name>
  <files>internal/scaling/drain.go</files>
  <action>
    Delete the UncordonNode method from internal/scaling/drain.go (lines 98-116, including the comment on line 98 and the blank line before it on line 97). This is the entire block:

    ```
    // UncordonNode marks a node as schedulable.
    func (d *drainHelper) UncordonNode(ctx context.Context, node *corev1.Node) error {
        ...
    }
    ```

    Do NOT touch CordonNode (lines 78-96) or DrainNode (line 118+). Only delete UncordonNode.

    After deletion, verify:
    1. grep -rn "UncordonNode" internal/ --include="*.go" returns zero matches
    2. grep -rn "CordonNode" internal/ --include="*.go" returns matches (CordonNode still exists and is called)
    3. go build ./... succeeds
    4. go test ./internal/scaling/... passes
  </action>
  <verify>
    grep -rn "UncordonNode" internal/ --include="*.go" | wc -l returns 0
    grep -rn "CordonNode" internal/ --include="*.go" | wc -l returns 2+ (definition + caller)
    go build ./...
    go test ./internal/scaling/...
  </verify>
  <done>
    UncordonNode does not exist in the codebase. CordonNode still exists with its caller in DrainNode. Build and tests pass.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "UncordonNode" . --include="*.go"` returns zero results
2. `grep -rn "CordonNode" internal/ --include="*.go"` returns at least 2 matches (definition + usage)
3. `go build ./...` exits 0
4. `go test ./internal/scaling/...` exits 0
</verification>

<success_criteria>
- UncordonNode method does not exist in drain.go or anywhere in the codebase
- CordonNode method still exists and has at least one caller
- go build ./... passes
- go test ./internal/scaling/... passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-dead-code-removal/13-01-SUMMARY.md`
</output>

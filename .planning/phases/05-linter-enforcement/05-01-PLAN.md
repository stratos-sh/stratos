---
phase: 05-linter-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .golangci.yml
  - cmd/stratos/main.go
  - internal/strategy/kubernetes/scaling.go
  - internal/controller/nodepool/cloud_sync.go
  - internal/controller/nodepool/lifecycle/warmup_monitor.go
  - internal/controller/nodepool/reconciler.go
  - internal/controller/nodeclass/reconciler.go
  - internal/github/client.go
autonomous: true

must_haves:
  truths:
    - "golangci-lint config enables depguard, funlen, cyclop, and contextcheck"
    - "depguard boundary rules block strategy/ from importing aws/ and controller sub-packages from importing provider implementations"
    - "funlen, cyclop, and contextcheck exclude test files"
    - "All errcheck, gosec, govet, misspell, staticcheck, and funlen violations are fixed"
  artifacts:
    - path: ".golangci.yml"
      provides: "Complete linter config with all 12 linters enabled"
      contains: "depguard"
    - path: ".golangci.yml"
      provides: "Depguard boundary rules"
      contains: "strategy-no-aws"
  key_links:
    - from: ".golangci.yml"
      to: "internal/strategy/"
      via: "depguard strategy-no-aws rule"
      pattern: "strategy-no-aws"
    - from: ".golangci.yml"
      to: "internal/controller/nodepool/lifecycle/"
      via: "depguard controller-sub-no-impl rule"
      pattern: "controller-sub-no-impl"
---

<objective>
Add all four structural linters (depguard, funlen, cyclop, contextcheck) to golangci-lint config and fix all non-complexity lint violations to bring the codebase to zero violations except for the 4 gocyclo/cyclop high-complexity functions.

Purpose: Establish the linter configuration that enforces package boundaries and code quality, then clear the backlog of simple violations so Plan 02 can focus exclusively on complexity refactoring.

Output: Updated .golangci.yml with 12 linters enabled, depguard boundary rules, and all errcheck/gosec/govet/misspell/staticcheck/funlen violations fixed.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-linter-enforcement/05-RESEARCH.md
@.golangci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .golangci.yml with four new linters and boundary rules</name>
  <files>.golangci.yml</files>
  <action>
Replace the entire `.golangci.yml` with the verified config from 05-RESEARCH.md. The config adds:

1. Four new linters to `linters.enable`: funlen, cyclop, depguard, contextcheck
2. New settings blocks for funlen (lines:80, statements:50, ignore-comments:true), cyclop (max-complexity:15, package-average:7.0), and depguard (two rule sets: strategy-no-aws and controller-sub-no-impl)
3. Three new test exclusions: funlen, cyclop, contextcheck added to the `_test\.go` exclusion rule

The complete config is in the RESEARCH.md "Complete .golangci.yml" section. Use it verbatim -- it has been verified via actual lint runs.

Do NOT remove gocyclo yet -- Plan 02 will decide whether to keep both or replace. For now, both gocyclo and cyclop are enabled.
  </action>
  <verify>Run `golangci-lint run --no-config --enable depguard,funlen,cyclop,contextcheck --disable-all 2>&1 | head -5` to confirm linters are available. Then `grep -c 'depguard\|funlen\|cyclop\|contextcheck' .golangci.yml` should return 8+ matches.</verify>
  <done>.golangci.yml contains all 12 linters with correct settings, boundary rules, and test exclusions.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all non-complexity lint violations (errcheck, gosec, govet, misspell, staticcheck, funlen)</name>
  <files>
    cmd/stratos/main.go
    internal/strategy/kubernetes/scaling.go
    internal/controller/nodepool/cloud_sync.go
    internal/controller/nodepool/lifecycle/warmup_monitor.go
    internal/controller/nodepool/reconciler.go
    internal/controller/nodeclass/reconciler.go
    internal/github/client.go
  </files>
  <action>
Fix all 21 non-complexity violations identified in the research. Run `make lint` first to get the exact current violation list, then fix each category:

**errcheck (9 violations):** Add error handling for unchecked return values in cloud_sync.go, warmup_monitor.go, github/client.go, scaling.go. For each: either handle the error (log + return/continue) or explicitly ignore with `_ =` if the error is truly unactionable. Prefer handling over ignoring.

**gosec (3 violations):** Fix integer overflow conversions in reconciler.go and nodeclass/reconciler.go. Use explicit bounds checking before int32/int64 conversions, or use `math.MaxInt32` guards. The pattern is typically `int(someInt32Value)` or vice versa -- add overflow guards.

**govet (3 violations):** Fix shadow declarations in reconciler.go. Rename the shadowing variable or restructure to avoid re-declaration with `:=` in inner scope. Common pattern: change inner `err :=` to `err =` when outer `err` already exists.

**misspell (3 violations):** Rename `strat` variable(s) in cloud_sync.go to `scalingStrategy` or `strategy`. The misspell linter flags `strat` as a misspelling of `start`.

**staticcheck (1 violation):** Fix unnecessary ObjectMeta selector in reconciler.go. Remove the redundant selector or replace with the simpler form.

**funlen (2 new violations):**
- `main()` in cmd/stratos/main.go (75 statements > 50): Extract setup logic into helper functions: `setupManager()`, `registerControllers()`, `startHealthChecks()` or similar. Each helper should be a focused function that `main()` calls sequentially.
- `CheckDemand` in internal/strategy/kubernetes/scaling.go (81 lines > 80): Extract a helper for the inner loop body or the result aggregation. A small extraction (even 10-15 lines into a helper) brings it under threshold.

Run `make lint` after ALL fixes to confirm only the 4 gocyclo/cyclop complexity violations remain (reconcileNodePool:46, LaunchInstance:19, MonitorWarmup:17, MonitorCloudWarmup:18).
  </action>
  <verify>Run `make lint 2>&1 | grep -v "gocyclo\|cyclop" | grep -c "error"` -- should be 0 (no non-complexity errors). Run `make lint 2>&1 | grep -c "gocyclo\|cyclop"` to confirm only complexity violations remain. Run `go build ./...` to confirm compilation.</verify>
  <done>Only 4 gocyclo/cyclop complexity violations remain. All errcheck, gosec, govet, misspell, staticcheck, and funlen violations are fixed. Code compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `.golangci.yml` contains depguard, funlen, cyclop, contextcheck in `linters.enable`
2. `.golangci.yml` has `strategy-no-aws` and `controller-sub-no-impl` depguard rules
3. `make lint` reports only gocyclo/cyclop complexity violations (4 functions), no other categories
4. `go build ./...` compiles cleanly
5. Depguard boundary rules pass (verified by running depguard alone)
</verification>

<success_criteria>
- .golangci.yml has 12 linters enabled with correct settings
- depguard rules enforce: strategy/ cannot import aws/, controller sub-packages cannot import aws/ or fake/
- Only 4 complexity violations remain (to be fixed in Plan 02)
- go build ./... passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-linter-enforcement/05-01-SUMMARY.md`
</output>

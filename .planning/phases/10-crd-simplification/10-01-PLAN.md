---
phase: 10-crd-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha1/strategy_types.go          # DELETE
  - api/v1alpha1/nodepool_types.go           # remove 2 fields
  - api/v1alpha1/zz_generated.deepcopy.go    # auto-regenerated
  - internal/controller/nodepool/nodepool_validation.go  # remove GHA validation block
  - internal/controller/nodepool/lifecycle/node_launch.go  # remove SkipKubernetesBootstrap line
  - internal/controller/nodepool/lifecycle/manager.go  # update comment
  - internal/controller/nodepool/doc.go  # update comment
  - internal/cloudprovider/interface.go  # remove SkipKubernetesBootstrap field
  - internal/cloudprovider/aws/provider.go  # remove SkipKubernetesBootstrap branch
  - deploy/charts/stratos/crds/stratos.sh_nodepools.yaml  # auto-regenerated
autonomous: true

must_haves:
  truths:
    - "NodePoolSpec has no ScalingStrategy or GitHubActions fields"
    - "strategy_types.go does not exist"
    - "Generated deepcopy code references no strategy types"
    - "CRD YAML contains no strategy fields"
    - "The entire project compiles cleanly"
  artifacts:
    - path: "api/v1alpha1/nodepool_types.go"
      provides: "Simplified NodePoolSpec without strategy fields"
      contains: "type NodePoolSpec struct"
      must_not_contain: "ScalingStrategy|GitHubActions"
    - path: "api/v1alpha1/zz_generated.deepcopy.go"
      provides: "Regenerated deepcopy without strategy types"
      must_not_contain: "GitHubActionsConfig|SecretReference"
    - path: "internal/cloudprovider/interface.go"
      provides: "TemplateConfig without SkipKubernetesBootstrap"
      must_not_contain: "SkipKubernetesBootstrap"
    - path: "deploy/charts/stratos/crds/stratos.sh_nodepools.yaml"
      provides: "CRD manifest without strategy fields"
      must_not_contain: "scalingStrategy|githubActions"
  key_links:
    - from: "api/v1alpha1/nodepool_types.go"
      to: "api/v1alpha1/zz_generated.deepcopy.go"
      via: "make generate (controller-gen)"
      pattern: "NodePoolSpec.*DeepCopyInto"
    - from: "internal/controller/nodepool/lifecycle/node_launch.go"
      to: "internal/cloudprovider/interface.go"
      via: "TemplateConfig struct usage"
      pattern: "cloudprovider\\.TemplateConfig"
---

<objective>
Remove all strategy-related fields from the NodePool CRD types and all consuming code, then regenerate deepcopy and CRD manifests.

Purpose: Complete CRD simplification -- the API surface reflects that Stratos is Kubernetes-only with no strategy abstraction.
Output: Clean NodePoolSpec with no ScalingStrategy/GitHubActions fields, no strategy_types.go, regenerated deepcopy and CRD YAML, full compilation passing.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-crd-simplification/10-RESEARCH.md

@api/v1alpha1/nodepool_types.go
@api/v1alpha1/strategy_types.go
@internal/controller/nodepool/nodepool_validation.go
@internal/controller/nodepool/lifecycle/node_launch.go
@internal/controller/nodepool/lifecycle/manager.go
@internal/controller/nodepool/doc.go
@internal/cloudprovider/interface.go
@internal/cloudprovider/aws/provider.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete strategy types and remove all references</name>
  <files>
    api/v1alpha1/strategy_types.go
    api/v1alpha1/nodepool_types.go
    internal/controller/nodepool/nodepool_validation.go
    internal/controller/nodepool/lifecycle/node_launch.go
    internal/controller/nodepool/lifecycle/manager.go
    internal/controller/nodepool/doc.go
    internal/cloudprovider/interface.go
    internal/cloudprovider/aws/provider.go
  </files>
  <action>
    Execute these changes in order. All must happen before `make generate`.

    1. **Delete `api/v1alpha1/strategy_types.go`** entirely. This file contains `ScalingStrategyType`, `ScalingStrategyKubernetes`, `ScalingStrategyGitHubActions`, `GitHubActionsConfig`, `SecretReference`, and `GetIdleTimeout()`. Delete the whole file.

    2. **Remove 2 fields from `api/v1alpha1/nodepool_types.go`**: Delete lines 60-71 (the `ScalingStrategy` field with its comment and kubebuilder markers, AND the `GitHubActions` field with its comment and kubebuilder marker). These are the last two fields in `NodePoolSpec` before the closing brace. After removal, `ScaleUp *ScaleUpConfig` (line 58) becomes the last field before `}`.

    3. **Remove validation block from `internal/controller/nodepool/nodepool_validation.go`**: Delete lines 49-64 (the entire `if nodePool.Spec.ScalingStrategy == stratosv1alpha1.ScalingStrategyGitHubActions { ... }` block). The function should go directly from the `supportedNodeClassKinds` check to `return nil`. Also remove the `stratosv1alpha1` import alias IF it becomes unused (check -- it is still used for `stratosv1alpha1.NodePool` in function signatures, so keep the import).

    4. **Remove `SkipKubernetesBootstrap` field from `internal/cloudprovider/interface.go`**: Delete line 31 (`SkipKubernetesBootstrap bool`) and its comment from the `TemplateConfig` struct. The struct should have 3 fields: `Labels`, `Taints`, `EnableNetworkReadinessTaint`.

    5. **Remove SkipKubernetesBootstrap line from `internal/controller/nodepool/lifecycle/node_launch.go`**: Delete line 45 (`SkipKubernetesBootstrap: pool.Spec.ScalingStrategy != "" && ...`). The `TemplateConfig` initialization should have 3 fields: Labels, Taints, EnableNetworkReadinessTaint. Also remove the `stratosv1alpha1` import IF it becomes unused (check -- it IS used on line 29 in the import and on line 37 for `stratosv1alpha1.NodePool` and `stratosv1alpha1.NodeClass`, so keep the import).

    6. **Remove SkipKubernetesBootstrap branch from `internal/cloudprovider/aws/provider.go`**: Delete lines 213-220 (the entire `if templateConfig != nil && templateConfig.SkipKubernetesBootstrap { ... }` block in `setUserData()`). The function should go directly to the `if p.clusterConfig != nil` check.

    7. **Update comment in `internal/controller/nodepool/lifecycle/manager.go`**: Change line 41 from `// Both KubernetesStrategy and GitHubActionsStrategy implement this interface.` to `// The scaling.Strategy implements this interface.`

    8. **Update comment in `internal/controller/nodepool/doc.go`**: Change line 27 from `// monitoring, pool maintenance, demand checking (via ScalingStrategy),` to `// monitoring, pool maintenance, demand checking (via scaling.Strategy),`

    IMPORTANT: Do NOT manually edit `zz_generated.deepcopy.go`. It will be regenerated in Task 2.

    IMPORTANT: Do NOT rename any test variables named `scalingStrategy` -- those are local variable names, not CRD references.
  </action>
  <verify>
    Grep confirms zero matches in Go source files (excluding zz_generated and .planning/):
    - `grep -r "ScalingStrategy\|GitHubActions\|SkipKubernetesBootstrap" --include="*.go" . | grep -v zz_generated | grep -v .planning/ | grep -v _test.go` returns empty
    - `ls api/v1alpha1/strategy_types.go` returns "No such file"
  </verify>
  <done>
    strategy_types.go deleted. ScalingStrategy and GitHubActions fields removed from NodePoolSpec. All 6 consuming code sites cleaned (validation block, launch line, cloudprovider field, aws branch, 2 comments).
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate code and verify full build</name>
  <files>
    api/v1alpha1/zz_generated.deepcopy.go
    deploy/charts/stratos/crds/stratos.sh_nodepools.yaml
  </files>
  <action>
    Run code generation and build verification in sequence:

    1. Run `make generate` to regenerate `zz_generated.deepcopy.go`. This removes the stale `GitHubActionsConfig.DeepCopyInto()`, `GitHubActionsConfig.DeepCopy()`, `SecretReference.DeepCopyInto()`, `SecretReference.DeepCopy()` methods, and the `GitHubActions` field copy in `NodePoolSpec.DeepCopyInto()`.

    2. Run `make manifests` to regenerate CRD YAML. The CRD YAML at `deploy/charts/stratos/crds/stratos.sh_nodepools.yaml` already does not contain strategy fields (research confirmed zero grep matches), so this step maintains the status quo but ensures consistency.

    3. Run `go build ./...` to verify the entire project compiles with no errors.

    4. Verify the regenerated files:
       - `grep -c "GitHubActionsConfig\|SecretReference" api/v1alpha1/zz_generated.deepcopy.go` should return 0
       - `grep -c "scalingStrategy\|githubActions" deploy/charts/stratos/crds/stratos.sh_nodepools.yaml` should return 0
  </action>
  <verify>
    - `make generate` exits 0
    - `make manifests` exits 0
    - `go build ./...` exits 0
    - `grep -c "GitHubActionsConfig\|SecretReference" api/v1alpha1/zz_generated.deepcopy.go` returns 0
    - `grep -c "scalingStrategy\|githubActions" deploy/charts/stratos/crds/stratos.sh_nodepools.yaml` returns 0
  </verify>
  <done>
    Deepcopy regenerated without strategy types. CRD manifest regenerated. Full project builds cleanly. All 5 success criteria from the roadmap are satisfied.
  </done>
</task>

</tasks>

<verification>
All 5 phase success criteria from ROADMAP.md:
1. `NodePoolSpec` struct has no `ScalingStrategy` field and no `GitHubActions` field -- verify by grep on nodepool_types.go
2. `api/v1alpha1/strategy_types.go` does not exist -- verify by `ls`
3. `zz_generated.deepcopy.go` contains no references to `GitHubActionsConfig` or strategy types -- verify by grep
4. CRD YAML reflects simplified spec -- verify by grep on stratos.sh_nodepools.yaml
5. `go build ./...` succeeds after `make generate && make manifests` -- verify by exit code
</verification>

<success_criteria>
- `api/v1alpha1/strategy_types.go` does not exist
- `grep "ScalingStrategy\|GitHubActions" api/v1alpha1/nodepool_types.go` returns empty
- `grep "GitHubActionsConfig\|SecretReference" api/v1alpha1/zz_generated.deepcopy.go` returns empty
- `grep "SkipKubernetesBootstrap" internal/cloudprovider/interface.go` returns empty
- `grep "scalingStrategy\|githubActions" deploy/charts/stratos/crds/stratos.sh_nodepools.yaml` returns empty
- `go build ./...` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-crd-simplification/10-01-SUMMARY.md`
</output>

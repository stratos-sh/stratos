---
phase: 06-documentation-and-test-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "make test passes -- all unit tests work with the current package structure"
    - "make test-integration passes -- all integration tests compile and pass"
    - "Integration tests pass 3 consecutive times without flakes"
  artifacts: []
  key_links: []
---

<objective>
Verify and certify that all tests pass against the restructured codebase, fulfilling QUAL-03.

Purpose: Research confirmed tests are already green. This plan runs the full test suite 3 times to formally certify the test state as part of the phase success criteria. If any test fails (unlikely), fix it.

Output: Certified test results demonstrating all unit and integration tests pass without flakes.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-and-test-recovery/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run unit tests and certify results</name>
  <files></files>
  <action>
Run `make test` and capture the output. All unit tests should pass based on research verification.

If any test fails:
1. Read the failing test and the source it tests
2. Fix the issue (likely an import path that needs updating)
3. Re-run `make test` to confirm the fix

Record the test results (pass count, fail count, coverage) for the SUMMARY.
  </action>
  <verify>make test exits with code 0 (all tests pass)</verify>
  <done>All unit tests pass. Results recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Run integration tests 3 times and certify no flakes</name>
  <files></files>
  <action>
Run `make test-integration` three consecutive times with fresh runs. Record the seed and result of each run.

Expected: 71/72 pass, 1 skipped (CEL validation -- envtest limitation). This is the known-good state from research.

If any test fails on any run:
1. Check if it is a genuine failure or a flake
2. If flake: investigate root cause, fix timing/race condition
3. If genuine failure: read test + source, fix the issue
4. Re-run all 3 times after any fix

After 3 clean runs, record:
- Run 1: seed, pass/fail/skip counts, duration
- Run 2: seed, pass/fail/skip counts, duration
- Run 3: seed, pass/fail/skip counts, duration
  </action>
  <verify>
    make test-integration exits with code 0 on all 3 runs
    Each run shows 71 passed, 1 skipped, 0 failed (or similar)
  </verify>
  <done>Integration tests pass 3 consecutive times with different random seeds. No flakes detected. QUAL-03 certified.</done>
</task>

</tasks>

<verification>
- `make test` passes (exit code 0)
- `make test-integration` passes 3 consecutive times (exit code 0 each time)
- No test flakes observed across 3 runs
- `make lint` passes (exit code 0) -- regression check
</verification>

<success_criteria>
All unit tests pass via make test.
All integration tests pass 3 consecutive times via make test-integration with no flakes.
1 skipped test (CEL validation in envtest) is acceptable and expected.
make lint continues to pass with zero issues.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-and-test-recovery/06-02-SUMMARY.md`
</output>

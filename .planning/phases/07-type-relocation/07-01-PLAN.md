---
phase: 07-type-relocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/scaling/doc.go
  - internal/scaling/kubernetes.go
  - internal/scaling/scaling.go
  - internal/scaling/capacity.go
  - internal/scaling/drain.go
  - internal/scaling/drain_eviction.go
  - internal/scaling/events.go
  - internal/scaling/maintenance.go
  - internal/scaling/network.go
  - internal/scaling/pod_assignments.go
  - internal/scaling/readiness.go
  - internal/scaling/types.go
  - internal/scaling/events_test.go
  - internal/scaling/kubernetes_test.go
  - internal/scaling/maintenance_test.go
  - internal/scaling/network_test.go
  - internal/scaling/readiness_test.go
  - internal/scaling/startup_taints_test.go
  - internal/strategy/interface.go
autonomous: true

must_haves:
  truths:
    - "`internal/scaling/` package exists with `package scaling` declaration in every file"
    - "`ScalingDemand` and `ScaleDownCandidate` types are defined in `internal/scaling/types.go`"
    - "All 11 source files and 6 test files from `internal/strategy/kubernetes/` are present in `internal/scaling/`"
    - "`internal/strategy/interface.go` uses type aliases pointing to `internal/scaling/`"
    - "`go build ./...` succeeds with zero errors"
    - "`internal/scaling/` files do NOT import `internal/strategy` (no import cycle)"
  artifacts:
    - path: "internal/scaling/types.go"
      provides: "ScalingDemand and ScaleDownCandidate type definitions"
      contains: "type ScalingDemand struct"
    - path: "internal/scaling/doc.go"
      provides: "Package documentation for scaling package"
      contains: "package scaling"
    - path: "internal/scaling/scaling.go"
      provides: "CheckDemand, OnScaleUp, FindScaleDownCandidates methods"
      contains: "package scaling"
    - path: "internal/scaling/kubernetes.go"
      provides: "Strategy struct, New constructor, DrainAndStop"
      contains: "package scaling"
    - path: "internal/strategy/interface.go"
      provides: "Type aliases for backward compatibility"
      contains: "type ScalingDemand = scaling.ScalingDemand"
  key_links:
    - from: "internal/strategy/interface.go"
      to: "internal/scaling/types.go"
      via: "type aliases (ScalingDemand, ScaleDownCandidate)"
      pattern: "type ScalingDemand = scaling\\.ScalingDemand"
    - from: "internal/scaling/scaling.go"
      to: "internal/scaling/types.go"
      via: "direct reference (local package types)"
      pattern: "ScalingDemand\\{|ScaleDownCandidate\\{"
---

<objective>
Create the `internal/scaling/` package by copying all files from `internal/strategy/kubernetes/`, updating package declarations, relocating `ScalingDemand` and `ScaleDownCandidate` types, and adding type aliases in the old package so the build passes without changing any consumers.

Purpose: This is the first step of removing the ScalingStrategy abstraction. By creating the target package as an additive change with type aliases, subsequent phases can incrementally rewire consumers and delete the old packages without big-bang breakage.

Output: A new `internal/scaling/` package with all Kubernetes scaling logic and shared types, plus type aliases in `internal/strategy/interface.go` so the codebase compiles unchanged.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-type-relocation/07-RESEARCH.md
@internal/strategy/interface.go
@internal/strategy/kubernetes/doc.go
@internal/strategy/kubernetes/kubernetes.go
@internal/strategy/kubernetes/scaling.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal/scaling/ package with all files and types</name>
  <files>
    internal/scaling/doc.go
    internal/scaling/kubernetes.go
    internal/scaling/scaling.go
    internal/scaling/capacity.go
    internal/scaling/drain.go
    internal/scaling/drain_eviction.go
    internal/scaling/events.go
    internal/scaling/maintenance.go
    internal/scaling/network.go
    internal/scaling/pod_assignments.go
    internal/scaling/readiness.go
    internal/scaling/types.go
    internal/scaling/events_test.go
    internal/scaling/kubernetes_test.go
    internal/scaling/maintenance_test.go
    internal/scaling/network_test.go
    internal/scaling/readiness_test.go
    internal/scaling/startup_taints_test.go
  </files>
  <action>
    1. Create `internal/scaling/` directory.

    2. Copy all 17 files from `internal/strategy/kubernetes/` to `internal/scaling/`:
       ```
       cp -r internal/strategy/kubernetes/* internal/scaling/
       ```

    3. Update package declarations in ALL copied `.go` files (both source and test):
       - Replace `package kubernetes` with `package scaling` in every file.
       - There are 11 source files and 6 test files, all using `package kubernetes`.

    4. Create `internal/scaling/types.go` with the ScalingDemand and ScaleDownCandidate type definitions:
       ```go
       package scaling

       import corev1 "k8s.io/api/core/v1"

       // ScalingDemand describes how many nodes should be started for scaling.
       type ScalingDemand struct {
           // NodesNeeded is the number of standby nodes to start.
           NodesNeeded int
           // Metadata carries strategy-specific context (e.g., matched pods, queued jobs).
           Metadata interface{}
       }

       // ScaleDownCandidate wraps a node that is eligible for scale-down.
       type ScaleDownCandidate struct {
           Node corev1.Node
       }
       ```

    5. Update `internal/scaling/doc.go`: Rewrite the package documentation to describe the scaling package (not "Package kubernetes"). The package provides Kubernetes pod-demand scaling -- computing node demand from unschedulable pods, drain orchestration, network readiness, and startup taint management.

    6. Fix imports in `internal/scaling/scaling.go`:
       - Remove the `"github.com/stratos-sh/stratos/internal/strategy"` import line.
       - Replace all `strategy.ScalingDemand` with `ScalingDemand` (14 occurrences).
       - Replace all `strategy.ScaleDownCandidate` with `ScaleDownCandidate` (5 occurrences).

    7. Fix imports in `internal/scaling/kubernetes.go`:
       - Remove the `"github.com/stratos-sh/stratos/internal/strategy"` import line.
       - Replace `strategy.ScaleDownCandidate` with `ScaleDownCandidate` (1 occurrence in the DrainAndStop method signature).

    8. Verify no file in `internal/scaling/` imports `internal/strategy`:
       ```
       grep -r 'internal/strategy' internal/scaling/
       ```
       Must return zero matches.
  </action>
  <verify>
    - `grep -rn 'package kubernetes' internal/scaling/` returns zero matches (all files use `package scaling`).
    - `grep -rn 'internal/strategy' internal/scaling/` returns zero matches (no import cycle risk).
    - `internal/scaling/types.go` exists and contains `type ScalingDemand struct` and `type ScaleDownCandidate struct`.
    - Count files: `ls internal/scaling/*.go | wc -l` returns 18 (11 source + 6 test + 1 types.go).
  </verify>
  <done>All 18 files exist in `internal/scaling/` with correct `package scaling` declarations, types are defined locally, and no `internal/strategy` imports remain in the new package.</done>
</task>

<task type="auto">
  <name>Task 2: Add type aliases in strategy/interface.go and verify build</name>
  <files>internal/strategy/interface.go</files>
  <action>
    1. Edit `internal/strategy/interface.go`:
       - Add import: `"github.com/stratos-sh/stratos/internal/scaling"`
       - Replace the ScalingDemand struct definition (lines 30-37) with a type alias:
         ```go
         // ScalingDemand is an alias for scaling.ScalingDemand during migration.
         type ScalingDemand = scaling.ScalingDemand
         ```
       - Replace the ScaleDownCandidate struct definition (lines 40-42) with a type alias:
         ```go
         // ScaleDownCandidate is an alias for scaling.ScaleDownCandidate during migration.
         type ScaleDownCandidate = scaling.ScaleDownCandidate
         ```
       - The `corev1` import is no longer needed in this file (it was only used by ScaleDownCandidate's Node field). Remove it.
       - The `ScalingStrategy` interface and its method signatures remain UNCHANGED -- they reference `ScalingDemand` and `ScaleDownCandidate` which are now aliases and resolve to the same types.

    2. Run `go build ./...` from the project root. It MUST succeed with zero errors.
       - If build fails, diagnose and fix. Common issues: missing alias `=` sign, leftover imports, package declaration mismatch.

    3. Run `go vet ./internal/scaling/...` to check for any issues.
  </action>
  <verify>
    - `go build ./...` exits with code 0 (no errors).
    - `go vet ./internal/scaling/...` exits with code 0.
    - `grep 'type ScalingDemand = scaling.ScalingDemand' internal/strategy/interface.go` returns a match.
    - `grep 'type ScaleDownCandidate = scaling.ScaleDownCandidate' internal/strategy/interface.go` returns a match.
  </verify>
  <done>`go build ./...` passes. The `internal/strategy/interface.go` file contains type aliases. All existing consumers (githubactions, controller/nodepool) compile without any changes.</done>
</task>

</tasks>

<verification>
1. `go build ./...` passes with zero errors (critical -- the codebase must compile).
2. `ls internal/scaling/*.go | wc -l` returns 18 files.
3. `grep -rn 'package scaling' internal/scaling/*.go | wc -l` returns 18 (every file has correct package declaration).
4. `grep -rn 'internal/strategy' internal/scaling/` returns zero matches (no import cycle).
5. `grep 'type ScalingDemand = scaling.ScalingDemand' internal/strategy/interface.go` confirms alias exists.
6. `grep 'type ScaleDownCandidate = scaling.ScaleDownCandidate' internal/strategy/interface.go` confirms alias exists.
7. `internal/strategy/kubernetes/` is UNTOUCHED (still exists with original files -- deletion is Phase 9).
</verification>

<success_criteria>
- The `internal/scaling/` package exists with all 18 Go files (11 source + 6 test + 1 types.go).
- `ScalingDemand` and `ScaleDownCandidate` are defined in `internal/scaling/types.go` and importable from there.
- `internal/strategy/interface.go` contains type aliases pointing to the new types.
- `go build ./...` succeeds -- the entire codebase compiles without errors.
- `internal/strategy/kubernetes/` still exists unchanged (additive phase -- no deletions).
</success_criteria>

<output>
After completion, create `.planning/phases/07-type-relocation/07-01-SUMMARY.md`
</output>

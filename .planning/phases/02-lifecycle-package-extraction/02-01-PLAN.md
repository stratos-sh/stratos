---
phase: 02-lifecycle-package-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/lifecycle/warmup.go
  - internal/controller/lifecycle/warmup_monitor.go
  - internal/controller/lifecycle/warmup_handlers.go
  - internal/controller/lifecycle/warmup_adoption.go
autonomous: true

must_haves:
  truths:
    - "warmup.go no longer exists -- replaced by three focused files"
    - "warmup_monitor.go contains exactly MonitorWarmup and MonitorCloudWarmup"
    - "warmup_handlers.go contains all timeout and failure handlers"
    - "warmup_adoption.go contains adoptAndTransitionToStandby"
    - "warmup_monitor.go is ~215 lines (user locked decision to keep both monitors together; 181 lines of code + 34 lines license/package/imports overhead)"
    - "warmup_handlers.go is ~206 lines (4 handlers totaling 170 lines of code + overhead; no further split warranted for a 455-line source)"
    - "warmup_adoption.go is under 100 lines"
    - "No file exceeds 220 lines"
    - "All unit tests pass unchanged"
  artifacts:
    - path: "internal/controller/lifecycle/warmup_monitor.go"
      provides: "Warmup monitoring entry points (MonitorWarmup, MonitorCloudWarmup)"
      contains: "func (m *Manager) MonitorWarmup"
    - path: "internal/controller/lifecycle/warmup_handlers.go"
      provides: "Warmup timeout, failure, and completion handlers"
      contains: "func (m *Manager) handleWarmupTimeout"
    - path: "internal/controller/lifecycle/warmup_adoption.go"
      provides: "Node adoption flow"
      contains: "func (m *Manager) adoptAndTransitionToStandby"
  key_links:
    - from: "warmup_monitor.go"
      to: "warmup_handlers.go"
      via: "cross-file method calls within same package"
      pattern: "m\\.handleWarmupTimeout|m\\.handleControllerStopWarmup|m\\.handleWarmupFailure|m\\.handleCloudWarmupTimeout"
    - from: "warmup_monitor.go"
      to: "warmup_adoption.go"
      via: "cross-file method call within same package"
      pattern: "m\\.adoptAndTransitionToStandby"
---

<objective>
Split warmup.go (455 lines, 7 methods) into three focused files organized by responsibility: monitoring entry points, handlers, and adoption flow.

Purpose: warmup.go is the largest file in lifecycle/ and mixes public monitoring entry points with private handler logic and adoption flows. Splitting by responsibility makes each file easier to navigate and maintain.
Output: Three new files (warmup_monitor.go, warmup_handlers.go, warmup_adoption.go) replacing warmup.go. No behavioral changes.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/controller/lifecycle/warmup.go
@internal/controller/lifecycle/manager.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split warmup.go into three files</name>
  <files>
    internal/controller/lifecycle/warmup.go
    internal/controller/lifecycle/warmup_monitor.go
    internal/controller/lifecycle/warmup_handlers.go
    internal/controller/lifecycle/warmup_adoption.go
  </files>
  <action>
Split warmup.go into three new files and delete the original. This is pure code movement within the same Go package -- no logic changes, no signature changes.

**warmup_monitor.go** -- the two public monitoring entry points:
- Copy the Apache 2.0 license header (lines 1-15 from warmup.go, identical across all files)
- `package lifecycle`
- Add only the imports needed by these two methods
- `MonitorWarmup` (currently lines 35-127 of warmup.go, 93 lines including comment)
- `MonitorCloudWarmup` (currently lines 239-326 of warmup.go, 88 lines including comments)
- Expected size: ~215 lines (181 lines of code + 34 lines overhead: 15 license, 3 blank/package/blank, 14 imports, 1 blank after imports, 1 blank separator between functions). Exceeds 200-line target but accepted per user's locked decision to keep both monitors together (see 02-CONTEXT.md)

**warmup_handlers.go** -- all private timeout/completion/failure handlers:
- Same license header and package declaration
- Add only the imports needed by these four methods
- `handleWarmupTimeout` (currently lines 129-166, 38 lines)
- `handleControllerStopWarmup` (currently lines 168-237, 70 lines)
- `handleWarmupFailure` (currently lines 328-349, 22 lines)
- `handleCloudWarmupTimeout` (currently lines 351-390, 40 lines)
- Expected size: ~206 lines (170 lines of code + 3 blank separators + 33 lines overhead). Slightly over 200-line target; acceptable given the alternative is 4+ handler files for a 455-line source

**warmup_adoption.go** -- the node adoption flow:
- Same license header and package declaration
- Add only the imports needed by this one method
- `adoptAndTransitionToStandby` (currently lines 392-455, 64 lines including comments)
- Expected size: ~96 lines (64 lines code + 32 lines overhead)

After creating all three files, DELETE warmup.go entirely.

**Critical details:**
- Each file must have the identical Apache 2.0 license header from the existing files
- Each file must have `package lifecycle` declaration
- Each file needs only its own imports -- do NOT copy the full import block from warmup.go. Determine which imports each method actually uses:
  - warmup_monitor.go needs: context, fmt, time, corev1, client, log, stratosv1alpha1, cloudprovider, nodestate, metrics (no strings -- neither monitor uses it directly)
  - warmup_handlers.go needs: context, fmt, time, corev1, client, log, stratosv1alpha1, cloudprovider, nodestate, metrics
  - warmup_adoption.go needs: context, fmt, strings, time, corev1, nodestate, client, log, stratosv1alpha1
- Do NOT modify warmup_test.go -- it tests functions across source files and works fine in Go
- Do NOT change any function signatures, logic, or comments
  </action>
  <verify>
Run these commands in sequence:
1. `go build ./internal/controller/lifecycle/` -- must compile with no errors
2. `go test -v -count=1 ./internal/controller/lifecycle/` -- all tests must pass
3. `wc -l internal/controller/lifecycle/warmup_monitor.go internal/controller/lifecycle/warmup_handlers.go internal/controller/lifecycle/warmup_adoption.go` -- warmup_monitor.go ~215 lines (accepted: user locked both monitors together), warmup_handlers.go ~206 lines, warmup_adoption.go ~96 lines
4. `test ! -f internal/controller/lifecycle/warmup.go && echo "warmup.go deleted"` -- original file must not exist
  </verify>
  <done>
warmup.go is replaced by three files: warmup_monitor.go (~215 lines, contains MonitorWarmup + MonitorCloudWarmup), warmup_handlers.go (~206 lines, contains all 4 handlers), warmup_adoption.go (~96 lines, contains adoptAndTransitionToStandby). All tests pass. No behavioral changes.
  </done>
</task>

</tasks>

<verification>
1. `go build ./internal/controller/lifecycle/` compiles cleanly
2. `go test -v -count=1 ./internal/controller/lifecycle/` passes all tests
3. No file exceeds 220 lines (`wc -l`) -- warmup_monitor.go ~215, warmup_handlers.go ~206, warmup_adoption.go ~96
4. warmup.go no longer exists
5. `go list -f '{{.Imports}}' ./internal/controller/lifecycle/ | grep -v nodestate | grep controller && echo "FAIL: upward import" || echo "PASS"` -- no upward imports
</verification>

<success_criteria>
- warmup.go deleted and replaced by warmup_monitor.go, warmup_handlers.go, warmup_adoption.go
- No file exceeds 220 lines (warmup_monitor.go ~215 accepted per user's locked decision; warmup_handlers.go ~206; warmup_adoption.go well under)
- Package compiles and all tests pass
- No behavioral changes -- pure file reorganization
</success_criteria>

<output>
After completion, create `.planning/phases/02-lifecycle-package-extraction/02-01-SUMMARY.md`
</output>

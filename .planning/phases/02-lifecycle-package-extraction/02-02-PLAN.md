---
phase: 02-lifecycle-package-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/lifecycle/operations.go
  - internal/controller/lifecycle/node_launch.go
  - internal/controller/lifecycle/node_startstop.go
  - internal/controller/lifecycle/node_sync.go
autonomous: true

must_haves:
  truths:
    - "operations.go no longer exists -- replaced by three focused files"
    - "node_launch.go contains LaunchNode and LabelNode"
    - "node_startstop.go contains StartNode, StopNode, and TransitionState"
    - "node_sync.go contains SyncNodeState, FindNodeByInstanceID, deleteNode, and setLastStartedAnnotation"
    - "No file exceeds 200 lines"
    - "All unit tests pass unchanged"
  artifacts:
    - path: "internal/controller/lifecycle/node_launch.go"
      provides: "Instance provisioning and node labeling"
      contains: "func (m *Manager) LaunchNode"
    - path: "internal/controller/lifecycle/node_startstop.go"
      provides: "Instance start/stop and state transitions"
      contains: "func (m *Manager) StartNode"
    - path: "internal/controller/lifecycle/node_sync.go"
      provides: "State sync, node discovery, and cleanup"
      contains: "func (m *Manager) SyncNodeState"
  key_links:
    - from: "node_startstop.go"
      to: "node_startstop.go"
      via: "StartNode and StopNode both call TransitionState (same file)"
      pattern: "m\\.TransitionState"
    - from: "node_sync.go"
      to: "node_startstop.go"
      via: "SyncNodeState calls TransitionState (cross-file, same package)"
      pattern: "m\\.TransitionState"
    - from: "node_sync.go"
      to: "manager.go"
      via: "FindNodeByInstanceID calls containsInstanceID helper"
      pattern: "containsInstanceID"
---

<objective>
Split operations.go (355 lines, 9 methods) into three focused files organized by lifecycle concern: instance provisioning, start/stop transitions, and state synchronization.

Purpose: operations.go mixes launch, transition, sync, and cleanup logic into one file. Splitting by concern makes each file focused on a single lifecycle domain.
Output: Three new files (node_launch.go, node_startstop.go, node_sync.go) replacing operations.go. No behavioral changes.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/controller/lifecycle/operations.go
@internal/controller/lifecycle/manager.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split operations.go into three files</name>
  <files>
    internal/controller/lifecycle/operations.go
    internal/controller/lifecycle/node_launch.go
    internal/controller/lifecycle/node_startstop.go
    internal/controller/lifecycle/node_sync.go
  </files>
  <action>
Split operations.go into three new files and delete the original. This is pure code movement within the same Go package -- no logic changes, no signature changes.

**node_launch.go** -- instance provisioning concern:
- Copy the Apache 2.0 license header (lines 1-15 from operations.go, identical across all files)
- `package lifecycle`
- Add only the imports needed by these two methods
- `LaunchNode` (currently lines 39-68 of operations.go)
- `LabelNode` (currently lines 74-115 of operations.go)
- Expected size: ~102 lines

**node_startstop.go** -- instance lifecycle transitions:
- Same license header and package declaration
- Add only the imports needed by these three methods
- `TransitionState` (currently lines 118-140) -- placed FIRST because StartNode and StopNode call it
- `StartNode` (currently lines 143-198)
- `StopNode` (currently lines 201-250)
- Expected size: ~159 lines

**node_sync.go** -- discovery, sync, and cleanup:
- Same license header and package declaration
- Add only the imports needed by these four methods
- `SyncNodeState` (currently lines 265-315)
- `FindNodeByInstanceID` (currently lines 319-340)
- `deleteNode` (currently lines 253-262)
- `setLastStartedAnnotation` (currently lines 344-355)
- Expected size: ~125 lines

After creating all three files, DELETE operations.go entirely.

**Critical details:**
- Each file must have the identical Apache 2.0 license header from the existing files
- Each file must have `package lifecycle` declaration
- Each file needs only its own imports -- do NOT copy the full import block from operations.go. Determine which imports each method actually uses:
  - node_launch.go needs: context, fmt, strings, time, corev1, client, log, stratosv1alpha1, cloudprovider, nodestate, metrics (note: LabelNode uses strings for HasPrefix check)
  - node_startstop.go needs: context, fmt, time, corev1, client, log, stratosv1alpha1, cloudprovider, nodestate, metrics
  - node_sync.go needs: context, fmt, time, corev1, apierrors "k8s.io/apimachinery/pkg/api/errors", client, log, stratosv1alpha1, cloudprovider, nodestate
- Do NOT modify warmup_test.go -- it tests functions across source files and works fine in Go
- Do NOT change any function signatures, logic, or comments
- `deleteNode` is private and called from both node_sync.go methods AND warmup methods -- this works fine cross-file because all files share the same package namespace
  </action>
  <verify>
Run these commands in sequence:
1. `go build ./internal/controller/lifecycle/` -- must compile with no errors
2. `go test -v -count=1 ./internal/controller/lifecycle/` -- all tests must pass
3. `wc -l internal/controller/lifecycle/node_launch.go internal/controller/lifecycle/node_startstop.go internal/controller/lifecycle/node_sync.go` -- all files under 200 lines
4. `test ! -f internal/controller/lifecycle/operations.go && echo "operations.go deleted"` -- original file must not exist
  </verify>
  <done>
operations.go is replaced by three files: node_launch.go (under 200 lines, contains LaunchNode + LabelNode), node_startstop.go (under 200 lines, contains TransitionState + StartNode + StopNode), node_sync.go (under 200 lines, contains SyncNodeState + FindNodeByInstanceID + deleteNode + setLastStartedAnnotation). All tests pass. No behavioral changes.
  </done>
</task>

</tasks>

<verification>
1. `go build ./internal/controller/lifecycle/` compiles cleanly
2. `go test -v -count=1 ./internal/controller/lifecycle/` passes all tests
3. All three new files are under 200 lines (`wc -l`)
4. operations.go no longer exists
5. `go list -f '{{.Imports}}' ./internal/controller/lifecycle/ | grep -v nodestate | grep controller && echo "FAIL: upward import" || echo "PASS"` -- no upward imports
6. `go list -f '{{.Imports}}' ./internal/controller/nodestate/ | grep controller && echo "FAIL" || echo "PASS"` -- nodestate still clean
</verification>

<success_criteria>
- operations.go deleted and replaced by node_launch.go, node_startstop.go, node_sync.go
- Every new file is under 200 lines
- Package compiles and all tests pass
- No behavioral changes -- pure file reorganization
</success_criteria>

<output>
After completion, create `.planning/phases/02-lifecycle-package-extraction/02-02-SUMMARY.md`
</output>

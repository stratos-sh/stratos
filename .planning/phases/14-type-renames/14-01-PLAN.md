---
phase: 14-type-renames
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # gopls rename: Strategy -> Scaler (cross-package)
  - internal/scaling/kubernetes.go
  - internal/scaling/scaling.go
  - internal/scaling/readiness.go
  - internal/scaling/maintenance.go
  - internal/scaling/pod_assignments.go
  - internal/scaling/kubernetes_test.go
  - internal/scaling/maintenance_test.go
  - internal/scaling/startup_taints_test.go
  - internal/scaling/readiness_test.go
  - internal/controller/nodepool/reconciler.go
  - internal/controller/nodepool/reconciler_helpers.go
  - internal/controller/nodepool/provider_cache.go
  # gopls rename: drainHelper -> nodeDrainer, drainConfig -> drainOptions
  - internal/scaling/drain.go
  - internal/scaling/drain_eviction.go
  # Comment updates (doc.go files, lifecycle)
  - internal/scaling/doc.go
  - internal/scaling/types.go
  - internal/controller/nodepool/doc.go
  - internal/controller/nodepool/setup.go
  - internal/controller/nodepool/lifecycle/manager.go
  - internal/controller/nodepool/lifecycle/warmup_handlers.go
  - internal/controller/doc.go
  - internal/cloudprovider/doc.go
  - internal/cloudprovider/fake/doc.go
autonomous: true

must_haves:
  truths:
    - "scaling.Scaler is used everywhere that previously said scaling.Strategy"
    - "nodeDrainer is used everywhere that previously said drainHelper"
    - "drainOptions is used everywhere that previously said drainConfig"
    - "No stale Strategy references remain (excluding NetworkReadinessStrategy and StrategyEnum)"
    - "All code compiles and all tests pass"
  artifacts:
    - path: "internal/scaling/kubernetes.go"
      provides: "Scaler struct definition (renamed from Strategy)"
      contains: "type Scaler struct"
    - path: "internal/scaling/drain.go"
      provides: "nodeDrainer and drainOptions types (renamed from drainHelper and drainConfig)"
      contains: "type nodeDrainer struct"
    - path: "internal/controller/nodepool/provider_cache.go"
      provides: "Updated compile-time interface assertion"
      contains: "(*scaling.Scaler)(nil)"
  key_links:
    - from: "internal/controller/nodepool/reconciler.go"
      to: "internal/scaling/kubernetes.go"
      via: "scaler field type"
      pattern: "scaler \\*scaling\\.Scaler"
    - from: "internal/controller/nodepool/provider_cache.go"
      to: "internal/scaling/kubernetes.go"
      via: "compile-time interface assertion"
      pattern: "scaling\\.Scaler.*lifecycle\\.NodeHooks"
    - from: "internal/scaling/kubernetes.go"
      to: "internal/scaling/drain.go"
      via: "nodeDrainer constructor call"
      pattern: "newNodeDrainer"
---

<objective>
Rename three types using gopls: Strategy -> Scaler, drainHelper -> nodeDrainer, drainConfig -> drainOptions. Then update all stale comments that gopls does not touch.

Purpose: Align type names with their actual roles after the ScalingStrategy interface was removed in v1.1. Strategy is a vestigial name -- this struct is a concrete Scaler, not an abstract strategy.
Output: All code, tests, and comments consistently use the new names. Zero stale references.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/scaling/kubernetes.go
@internal/scaling/drain.go
@internal/controller/nodepool/provider_cache.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gopls rename for all three types</name>
  <files>
    internal/scaling/kubernetes.go
    internal/scaling/scaling.go
    internal/scaling/readiness.go
    internal/scaling/maintenance.go
    internal/scaling/pod_assignments.go
    internal/scaling/drain.go
    internal/scaling/drain_eviction.go
    internal/scaling/kubernetes_test.go
    internal/scaling/maintenance_test.go
    internal/scaling/startup_taints_test.go
    internal/scaling/readiness_test.go
    internal/controller/nodepool/reconciler.go
    internal/controller/nodepool/reconciler_helpers.go
    internal/controller/nodepool/provider_cache.go
  </files>
  <action>
    Run three gopls rename commands sequentially. gopls is at ~/go/bin/gopls (v0.21.0). The PATH must include $(go env GOPATH)/bin.

    **Rename 1: Strategy -> Scaler** (exported, cross-package)
    The type definition is at internal/scaling/kubernetes.go:39. Run:
    ```
    gopls rename -w internal/scaling/kubernetes.go:39:6 Scaler
    ```
    The offset 39:6 points to the "S" in "Strategy" on `type Strategy struct`. gopls will atomically rename all code references across packages (struct literals, method receivers, parameter types, compile-time assertions). It will NOT touch comments.

    **Rename 2: drainHelper -> nodeDrainer** (unexported, package-internal)
    The type definition is at internal/scaling/drain.go:30. Run:
    ```
    gopls rename -w internal/scaling/drain.go:30:6 nodeDrainer
    ```
    The offset 30:6 points to the "d" in "drainHelper" on `type drainHelper struct`.

    **Rename 3: drainConfig -> drainOptions** (unexported, package-internal)
    The type definition is at internal/scaling/drain.go:41. Run:
    ```
    gopls rename -w internal/scaling/drain.go:41:6 drainOptions
    ```
    The offset 41:6 points to the "d" in "drainConfig" on `type drainConfig struct`. Note: after Rename 2 changes drain.go, the line number for drainConfig might shift. Read drain.go after Rename 2 completes to confirm the exact line for drainConfig before running Rename 3.

    Also rename the constructor and default functions that will still have stale names after gopls (gopls renames type references but the function names are independent symbols):
    - `newDrainHelper` -> `newNodeDrainer`: find its definition line in drain.go and run `gopls rename -w internal/scaling/drain.go:LINE:COL newNodeDrainer`
    - `defaultDrainConfig` -> `defaultDrainOptions`: find its definition line in drain.go and run `gopls rename -w internal/scaling/drain.go:LINE:COL defaultDrainOptions`

    After all renames, verify `go build ./...` compiles. If it fails, read the error and fix -- likely a missed reference.
  </action>
  <verify>
    Run `go build ./...` -- must succeed with zero errors.
    Run `go vet ./...` -- must succeed.
    Grep for stale code references (not comments):
    - `grep -rn "type Strategy struct" internal/` returns zero matches
    - `grep -rn "type drainHelper struct" internal/` returns zero matches
    - `grep -rn "type drainConfig struct" internal/` returns zero matches
    - `grep -rn "scaling\.Strategy" internal/ --include="*.go"` returns zero matches
  </verify>
  <done>All three type renames applied atomically via gopls. Code compiles. No stale type references in code.</done>
</task>

<task type="auto">
  <name>Task 2: Update stale comments and string literals</name>
  <files>
    internal/scaling/doc.go
    internal/scaling/kubernetes.go
    internal/scaling/types.go
    internal/scaling/drain.go
    internal/controller/nodepool/doc.go
    internal/controller/nodepool/reconciler.go
    internal/controller/nodepool/reconciler_helpers.go
    internal/controller/nodepool/setup.go
    internal/controller/nodepool/provider_cache.go
    internal/controller/nodepool/lifecycle/manager.go
    internal/controller/nodepool/lifecycle/warmup_handlers.go
    internal/controller/doc.go
    internal/cloudprovider/doc.go
    internal/cloudprovider/fake/doc.go
  </files>
  <action>
    gopls does NOT update comments or string literals. Update every comment that references the old type names. Read each file, find the stale comment, and update it.

    **Strategy -> Scaler comment sites (update "Strategy" to "Scaler" where it refers to the type):**

    1. `internal/scaling/doc.go:23` -- change `Strategy: core scaling logic` to `Scaler: core scaling logic`
    2. `internal/scaling/kubernetes.go:36` -- change `// Strategy scales NodePool nodes` to `// Scaler scales NodePool nodes` (gopls may have updated this godoc comment since it's directly above the type -- check first, only update if stale)
    3. `internal/scaling/kubernetes.go:47` -- change `// New creates a new Strategy.` to `// New creates a new Scaler.` (same caveat -- check first)
    4. `internal/controller/nodepool/doc.go:27` -- change `(via scaling.Strategy)` to `(via scaling.Scaler)`
    5. `internal/controller/nodepool/doc.go:35` -- change `to scaling.Strategy for demand` to `to scaling.Scaler for demand`
    6. `internal/controller/nodepool/provider_cache.go:102` -- change `scaling.Strategy implements` to `scaling.Scaler implements`
    7. `internal/controller/nodepool/lifecycle/manager.go:41` -- change `The scaling.Strategy implements` to `The scaling.Scaler implements`

    **"strategy" (lowercase) in comments -- update to "scaler" where it refers to the type/concept, but leave alone where "strategy" is used in a generic English sense:**

    8. `internal/controller/nodepool/reconciler_helpers.go:93` -- change `// Strategy maintenance` to `// Scaler maintenance`
    9. `internal/controller/nodepool/reconciler_helpers.go:95` -- change `"Failed to run strategy maintenance"` to `"Failed to run scaler maintenance"` (this is a log message string literal)
    10. `internal/controller/nodepool/reconciler_helpers.go:75` -- change `strategy maintenance` to `scaler maintenance` in the comment
    11. `internal/controller/nodepool/reconciler_helpers.go:59` -- change `notify strategy` to `notify scaler`
    12. `internal/controller/nodepool/reconciler.go:69` -- change `// scaler is the single scaling strategy` to `// scaler is the single pod-demand scaler`
    13. `internal/controller/nodepool/reconciler.go:159` -- change `scaling strategy` to `scaler`
    14. `internal/controller/nodepool/reconciler.go:226` -- change `strategy as NodeHooks` to `scaler as NodeHooks`
    15. `internal/controller/nodepool/setup.go:67` -- change `// Initialize the single scaling strategy` to `// Initialize the single pod-demand scaler`
    16. `internal/controller/nodepool/lifecycle/manager.go:67` -- change `strategy-provided NodeHooks` to `scaler-provided NodeHooks`
    17. `internal/controller/nodepool/lifecycle/warmup_handlers.go:77` -- change `// Use strategy-provided readiness checker` to `// Use scaler-provided readiness checker`
    18. `internal/controller/nodepool/lifecycle/warmup_handlers.go:84` -- change `(strategy check)` to `(scaler check)` in log message string
    19. `internal/scaling/types.go:26` -- change `strategy-specific context` to `scaler-specific context`
    20. `internal/controller/doc.go:21` -- change `strategy dependencies` to `scaler dependencies`
    21. `internal/cloudprovider/doc.go:35` -- change `strategy packages` to `scaler packages`
    22. `internal/cloudprovider/fake/doc.go:31` -- change `strategy tests` to `scaler tests`
    23. `internal/controller/nodepool/lifecycle/manager.go:40` -- change `strategy-specific` to `scaler-specific` if present in `NodeHooks provides strategy-specific`

    **drainHelper -> nodeDrainer comment sites:**

    24. `internal/scaling/drain.go` -- update comment `// drainHelper assists with draining nodes.` to `// nodeDrainer assists with draining nodes.` (gopls may have updated this godoc comment -- check first)
    25. `internal/scaling/drain.go` -- update comment `// drainConfig configures the drain helper.` to `// drainOptions configures the node drainer.` (check first)
    26. `internal/scaling/drain.go` -- update comment `// defaultDrainConfig returns the default drain configuration.` to `// defaultDrainOptions returns the default drain options.`
    27. `internal/scaling/drain.go` -- update comment `// newDrainHelper creates a new drain helper.` to `// newNodeDrainer creates a new node drainer.`

    **IMPORTANT exclusions -- do NOT rename these:**
    - `NetworkReadinessStrategy` and `NetworkReadinessStrategyNone`/`NetworkReadinessStrategyTaint` -- these are UNRELATED enum types in the CRD API
    - `StrategyEnum` -- unrelated type
    - Test variable names like `taintStrategy`, `scalingStrategy`, `testStrategy` -- these are local variable names, not type references. Leave them alone (the variable naming is fine).
    - Generic English uses of "strategy" in comments where it does not refer to the renamed type (use judgment -- if the comment says "scaling strategy" meaning "the Strategy struct", update it; if it means "approach/method" in English, leave it)
  </action>
  <verify>
    Run the definitive stale reference check from the success criteria:
    ```
    grep -rn "Strategy" internal/ --include="*.go" | grep -v NetworkReadinessStrategy | grep -v StrategyEnum | grep -v "stratosv1alpha1\." | grep -v "taintStrategy\|scalingStrategy\|testStrategy"
    ```
    This should return zero matches.

    Also check for stale drainHelper/drainConfig in comments:
    ```
    grep -rn "drainHelper\|drainConfig" internal/ --include="*.go"
    ```
    Should return zero matches.

    Run `go build ./...` and `go test ./...` to confirm nothing broke.
  </verify>
  <done>All comments and string literals updated to use new names. Zero stale references to Strategy (excluding NetworkReadinessStrategy/StrategyEnum), drainHelper, or drainConfig anywhere in internal/.</done>
</task>

<task type="auto">
  <name>Task 3: Full verification and test suite</name>
  <files></files>
  <action>
    Run the complete verification suite to confirm all renames are correct and nothing is broken.

    1. **Build check:** `go build ./...`
    2. **Vet check:** `go vet ./...`
    3. **Full test suite:** `go test ./...` -- all 163+ tests must pass
    4. **Stale reference grep (the definitive check from success criteria):**
       ```
       grep -rn "Strategy" internal/ --include="*.go" | grep -v NetworkReadinessStrategy | grep -v StrategyEnum
       ```
       This must return ONLY:
       - Lines containing `NetworkReadinessStrategy` variants (already excluded)
       - Lines containing `StrategyEnum` (already excluded)
       - Zero other matches

       If any matches remain, they are stale references. Fix them.
    5. **Stale drainHelper/drainConfig grep:**
       ```
       grep -rn "drainHelper\|drainConfig" internal/ --include="*.go"
       ```
       Must return zero matches.
    6. **Lint check:** `golangci-lint run ./...` if available, otherwise skip.
  </action>
  <verify>
    All five checks pass:
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - `go test ./...` exits 0 with all tests passing
    - Strategy grep returns zero non-excluded matches
    - drainHelper/drainConfig grep returns zero matches
  </verify>
  <done>All three renames verified: build green, all tests green, zero stale references. Phase 14 requirements TS-1, TS-2, TS-3 satisfied.</done>
</task>

</tasks>

<verification>
Phase 14 success criteria (from milestone roadmap):
1. `scaling.Scaler` used everywhere that previously said `scaling.Strategy` -- including compile-time assertion in `provider_cache.go`
2. `nodeDrainer` used everywhere that previously said `drainHelper` -- including struct literals in test files
3. `drainOptions` used everywhere that previously said `drainConfig` -- including constructor calls
4. `grep -rn "Strategy" internal/ --include="*.go" | grep -v NetworkReadinessStrategy | grep -v StrategyEnum` returns zero matches
5. `go build ./...` and `go test ./...` pass (all 163+ tests green)
</verification>

<success_criteria>
- `go build ./...` succeeds
- `go test ./...` passes all 163+ tests
- Zero stale "Strategy" references (excluding NetworkReadinessStrategy/StrategyEnum)
- Zero stale "drainHelper" or "drainConfig" references
- Comments consistently use Scaler, nodeDrainer, drainOptions
</success_criteria>

<output>
After completion, create `.planning/phases/14-type-renames/14-01-SUMMARY.md`
</output>

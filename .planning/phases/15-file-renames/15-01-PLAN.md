---
phase: 15-file-renames
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/scaling/scaler.go
  - internal/scaling/scaler_test.go
  - internal/scaling/pod_events.go
  - internal/scaling/pod_events_test.go
autonomous: true

must_haves:
  truths:
    - "internal/scaling/kubernetes.go no longer exists"
    - "internal/scaling/kubernetes_test.go no longer exists"
    - "internal/scaling/events.go no longer exists"
    - "internal/scaling/events_test.go no longer exists"
    - "internal/scaling/scaler.go exists with identical content"
    - "internal/scaling/scaler_test.go exists with identical content"
    - "internal/scaling/pod_events.go exists with identical content"
    - "internal/scaling/pod_events_test.go exists with identical content"
    - "git log --follow shows full history through the rename"
    - "go build ./... passes with zero changes beyond file renames"
  artifacts:
    - path: "internal/scaling/scaler.go"
      provides: "Renamed from kubernetes.go"
    - path: "internal/scaling/scaler_test.go"
      provides: "Renamed from kubernetes_test.go"
    - path: "internal/scaling/pod_events.go"
      provides: "Renamed from events.go"
    - path: "internal/scaling/pod_events_test.go"
      provides: "Renamed from events_test.go"
  key_links:
    - from: "git mv"
      to: "git log --follow"
      via: "rename-only commits preserve blame history"
      pattern: "git log --follow.*scaler\\.go"
---

<objective>
Rename scaling package source files to follow the subject_role.go naming convention.

Purpose: File names should reflect their primary type/concern. kubernetes.go contains the Scaler (renamed from KubernetesStrategy in Phase 14), so it should be scaler.go. events.go contains pod event handling, so it should be pod_events.go.

Output: Four files renamed via git mv, two rename-only commits (one per FR), go build passes, git blame preserved.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename kubernetes.go and kubernetes_test.go to scaler.go and scaler_test.go</name>
  <files>internal/scaling/scaler.go, internal/scaling/scaler_test.go</files>
  <action>
    Run these commands in sequence:

    1. `git mv internal/scaling/kubernetes.go internal/scaling/scaler.go`
    2. `git mv internal/scaling/kubernetes_test.go internal/scaling/scaler_test.go`
    3. `go build ./...` -- must pass with zero errors. File renames in Go have no compile impact because Go uses package paths, not file names.
    4. `go test ./internal/scaling/...` -- must pass. Tests reference types and functions, not file names.
    5. Commit with ONLY the renames staged (no other changes):
       ```
       git commit -m "rename(scaling): kubernetes.go -> scaler.go

       File follows its primary type (Scaler, renamed from KubernetesStrategy in Phase 14).
       Pure rename, no content changes -- git log --follow preserves full history."
       ```
    6. Verify: `git log --follow --oneline -5 internal/scaling/scaler.go` -- should show commits from before the rename.

    CRITICAL: Do NOT modify any file content. This must be a pure rename commit for git blame preservation. Phase 14 already did all content changes.
  </action>
  <verify>
    - `ls internal/scaling/kubernetes.go` returns "No such file"
    - `ls internal/scaling/scaler.go` returns the file
    - `ls internal/scaling/scaler_test.go` returns the file
    - `go build ./...` exits 0
    - `go test ./internal/scaling/...` exits 0
    - `git log --follow --oneline internal/scaling/scaler.go` shows history predating this commit
  </verify>
  <done>kubernetes.go renamed to scaler.go, kubernetes_test.go renamed to scaler_test.go, build passes, history preserved</done>
</task>

<task type="auto">
  <name>Task 2: Rename events.go and events_test.go to pod_events.go and pod_events_test.go</name>
  <files>internal/scaling/pod_events.go, internal/scaling/pod_events_test.go</files>
  <action>
    Run these commands in sequence:

    1. `git mv internal/scaling/events.go internal/scaling/pod_events.go`
    2. `git mv internal/scaling/events_test.go internal/scaling/pod_events_test.go`
    3. `go build ./...` -- must pass.
    4. `go test ./internal/scaling/...` -- must pass.
    5. Commit with ONLY the renames staged:
       ```
       git commit -m "rename(scaling): events.go -> pod_events.go

       File contains pod event handling, follows subject_role.go convention.
       Pure rename, no content changes -- git log --follow preserves full history."
       ```
    6. Verify: `git log --follow --oneline -5 internal/scaling/pod_events.go` -- should show commits from before the rename.

    CRITICAL: Do NOT modify any file content. Pure rename only.
  </action>
  <verify>
    - `ls internal/scaling/events.go` returns "No such file"
    - `ls internal/scaling/pod_events.go` returns the file
    - `ls internal/scaling/pod_events_test.go` returns the file
    - `go build ./...` exits 0
    - `go test ./internal/scaling/...` exits 0
    - `git log --follow --oneline internal/scaling/pod_events.go` shows history predating this commit
  </verify>
  <done>events.go renamed to pod_events.go, events_test.go renamed to pod_events_test.go, build passes, history preserved</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase:

1. Old files gone:
   - `! test -f internal/scaling/kubernetes.go`
   - `! test -f internal/scaling/kubernetes_test.go`
   - `! test -f internal/scaling/events.go`
   - `! test -f internal/scaling/events_test.go`

2. New files exist:
   - `test -f internal/scaling/scaler.go`
   - `test -f internal/scaling/scaler_test.go`
   - `test -f internal/scaling/pod_events.go`
   - `test -f internal/scaling/pod_events_test.go`

3. Build and tests pass:
   - `go build ./...`
   - `go test ./internal/scaling/...`

4. Git history preserved (both files):
   - `git log --follow --oneline internal/scaling/scaler.go` shows pre-rename history
   - `git log --follow --oneline internal/scaling/pod_events.go` shows pre-rename history

5. Exactly 2 new commits, each rename-only (no content diffs):
   - `git log --oneline -2` shows the two rename commits
   - `git diff HEAD~2 HEAD --stat` shows only renames, no insertions/deletions
</verification>

<success_criteria>
- Four files renamed (kubernetes.go -> scaler.go, kubernetes_test.go -> scaler_test.go, events.go -> pod_events.go, events_test.go -> pod_events_test.go)
- go build ./... passes
- go test ./internal/scaling/... passes
- git log --follow works for both renamed files
- Two clean rename-only commits (one per requirement FR-01 and FR-02)
- Zero content changes in either commit
</success_criteria>

<output>
After completion, create `.planning/phases/15-file-renames/15-01-SUMMARY.md`
</output>

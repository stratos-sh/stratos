---
phase: 12-clean-working-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha1/aws_nodeclass_types.go
  - api/v1alpha1/aws_nodeclass_types_test.go
  - api/v1alpha1/config_types.go
  - cmd/stratos/main.go
  - docs/sidebars.js
  - internal/cloudprovider/aws/ratelimit.go
  - internal/cloudprovider/fake/provider.go
  - internal/cloudprovider/fake/resolver.go
  - internal/controller/nodeclass/reconciler_test.go
  - internal/controller/nodepool/lifecycle/warmup_test.go
  - internal/controller/nodepool/nodepool_validation.go
  - internal/controller/nodepool/reconciler_helpers.go
  - internal/metrics/metrics.go
  - internal/scaling/readiness_test.go
  - internal/scaling/startup_taints_test.go
autonomous: true

must_haves:
  truths:
    - "git status shows no modified files -- working tree is clean"
    - "go build ./... succeeds on the committed state"
    - "All 15 files are committed on feat/network-readiness-strategy-enum branch"
  artifacts:
    - path: "git log HEAD~1..HEAD"
      provides: "Single commit containing all 15 modified files"
  key_links:
    - from: "committed state"
      to: "Phase 13+ refactoring"
      via: "clean working tree prerequisite"
      pattern: "git status.*clean"
---

<objective>
Commit all 15 unstaged modified files on the feat/network-readiness-strategy-enum branch so Phases 13-16 can start refactoring against a clean working tree.

Purpose: The v1.1.1 refactoring phases (dead code removal, type renames, file renames, struct changes) need a clean git baseline. These 15 files contain network-readiness-strategy-enum feature work that must be committed first.
Output: A single commit on the current branch with all 15 files staged and committed.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build, stage, and commit all modified files</name>
  <files>
    api/v1alpha1/aws_nodeclass_types.go
    api/v1alpha1/aws_nodeclass_types_test.go
    api/v1alpha1/config_types.go
    cmd/stratos/main.go
    docs/sidebars.js
    internal/cloudprovider/aws/ratelimit.go
    internal/cloudprovider/fake/provider.go
    internal/cloudprovider/fake/resolver.go
    internal/controller/nodeclass/reconciler_test.go
    internal/controller/nodepool/lifecycle/warmup_test.go
    internal/controller/nodepool/nodepool_validation.go
    internal/controller/nodepool/reconciler_helpers.go
    internal/metrics/metrics.go
    internal/scaling/readiness_test.go
    internal/scaling/startup_taints_test.go
  </files>
  <action>
    1. Run `go build ./...` to verify the current state compiles cleanly BEFORE committing.
    2. Stage all 15 modified files individually using `git add` with each file path explicitly listed (do NOT use `git add -A` or `git add .`).
    3. Commit with message: `feat: add network readiness strategy enum` followed by a body summarizing the changes -- enum type for network readiness strategy across API types, cloud provider, controller, and tests. Use the conventional commit style matching existing history (e.g., `feat:`, `chore:`, `docs:`).
    4. Run `go build ./...` again after commit to confirm the committed state builds.
    5. Run `git status` to confirm the working tree is clean.
  </action>
  <verify>
    - `go build ./...` exits 0
    - `git status` reports "nothing to commit, working tree clean"
    - `git log --oneline -1` shows the new commit
    - `git diff --stat HEAD~1..HEAD` shows exactly 15 files changed
  </verify>
  <done>
    All 15 modified files committed on feat/network-readiness-strategy-enum. Working tree is clean. Build passes. Phase 13+ can proceed without merge conflict risk.
  </done>
</task>

</tasks>

<verification>
- `go build ./...` exits 0
- `git status` shows clean working tree (no modified, staged, or untracked files in scope)
- `git log --oneline -1` shows the commit
- `git diff --stat HEAD~1..HEAD` shows 15 files changed, ~85 insertions, ~87 deletions
</verification>

<success_criteria>
1. `git status` shows no modified files in `internal/scaling/` or `internal/controller/nodepool/`
2. All 15 currently modified files are committed on the feature branch
3. `go build ./...` succeeds after the commit
</success_criteria>

<output>
After completion, create `.planning/phases/12-clean-working-tree/12-01-SUMMARY.md`
</output>

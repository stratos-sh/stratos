---
phase: 16-struct-field-type-change
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/scaling/types.go
  - internal/scaling/scaling.go
autonomous: true

must_haves:
  truths:
    - "ScalingDemand has a Pods []corev1.Pod field"
    - "ScalingDemand has no Metadata interface{} field"
    - "No type assertion demand.Metadata.([]corev1.Pod) exists in the codebase"
    - "All sites that set Metadata now set Pods directly"
    - "Build, tests, and lint pass clean"
  artifacts:
    - path: "internal/scaling/types.go"
      provides: "ScalingDemand struct with Pods field"
      contains: "Pods []corev1.Pod"
    - path: "internal/scaling/scaling.go"
      provides: "Direct field access instead of type assertion"
      pattern: "demand\\.Pods"
  key_links:
    - from: "internal/scaling/scaling.go:CheckDemand"
      to: "internal/scaling/types.go:ScalingDemand"
      via: "Pods field assignment"
      pattern: "Pods:\\s+unassignedPods"
    - from: "internal/scaling/scaling.go:OnScaleUp"
      to: "internal/scaling/types.go:ScalingDemand"
      via: "Direct Pods field access"
      pattern: "demand\\.Pods"
---

<objective>
Replace the unsafe `ScalingDemand.Metadata interface{}` field with a typed `Pods []corev1.Pod` field, eliminating the type assertion in OnScaleUp.

Purpose: Type safety -- the only value ever stored in Metadata is `[]corev1.Pod`. A concrete field makes this compiler-verified instead of runtime-checked.
Output: Two modified files in internal/scaling/ with zero behavior change.
</objective>

<execution_context>
@/home/roeeh/.claude/get-shit-done/workflows/execute-plan.md
@/home/roeeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/scaling/types.go
@internal/scaling/scaling.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Metadata field with Pods field and update all references</name>
  <files>internal/scaling/types.go, internal/scaling/scaling.go</files>
  <action>
  In `internal/scaling/types.go`:
  1. Replace the `Metadata interface{}` field and its comment with:
     ```go
     // Pods is the list of unschedulable pods that triggered the scale-up demand.
     Pods []corev1.Pod
     ```
  2. The `corev1` import already exists in types.go -- no import changes needed.

  In `internal/scaling/scaling.go`:
  1. At line ~95 in CheckDemand, change `Metadata: unassignedPods` to `Pods: unassignedPods`
  2. At lines ~138-141 in OnScaleUp, replace the type assertion block:
     ```go
     pods, ok := demand.Metadata.([]corev1.Pod)
     if !ok || len(pods) == 0 {
         return nil
     }
     ```
     with direct field access:
     ```go
     if len(demand.Pods) == 0 {
         return nil
     }
     ```
  3. At line ~143, change `s.createPodAssignments(ctx, nodePool, startedNodes, pods)` to `s.createPodAssignments(ctx, nodePool, startedNodes, demand.Pods)`

  Do NOT touch any other files. The `doc.go` comment about ScalingDemand is generic and does not mention Metadata, so it needs no update.
  </action>
  <verify>
  Run all three in sequence:
  ```bash
  go build ./...
  go test ./internal/scaling/...
  golangci-lint run ./...
  ```
  Also confirm no remnants:
  ```bash
  grep -rn "Metadata" internal/scaling/ --include="*.go"
  ```
  This grep must return zero results from scaling package Go files (the field and all references should be gone).
  </verify>
  <done>
  - `ScalingDemand` struct has `Pods []corev1.Pod` field, no `Metadata interface{}` field
  - `CheckDemand` sets `Pods: unassignedPods` (line ~95)
  - `OnScaleUp` uses `demand.Pods` directly with no type assertion (line ~138)
  - `go build ./...` passes
  - `go test ./internal/scaling/...` passes
  - `golangci-lint run ./...` passes clean
  - Zero occurrences of `Metadata` in `internal/scaling/*.go`
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles without errors
2. `go test ./internal/scaling/...` all tests pass
3. `golangci-lint run ./...` no warnings or errors
4. `grep -rn "Metadata" internal/scaling/ --include="*.go"` returns nothing
5. `grep -rn "interface{}" internal/scaling/types.go` returns nothing
6. `grep -n "Pods \[\]corev1.Pod" internal/scaling/types.go` returns the new field
</verification>

<success_criteria>
- ScalingDemand.Metadata interface{} is gone, replaced by Pods []corev1.Pod
- The type assertion demand.Metadata.([]corev1.Pod) is eliminated
- Build, tests, and lint all pass
- Zero behavior change -- same pods flow through, just type-safe now
</success_criteria>

<output>
After completion, create `.planning/phases/16-struct-field-type-change/16-01-SUMMARY.md`
</output>

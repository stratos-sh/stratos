# Requirements Archive: v1.1.1 Naming & Dead Code Cleanup

**Archived:** 2026-02-04
**Status:** SHIPPED

This is the archived requirements specification for v1.1.1.

---

## Table Stakes (v1 -- must ship)

### Dead Code

- [x] **TS-5**: Remove dead `UncordonNode` method from `drain.go` (zero callers, 18 lines, confirmed by `deadcode` tool)

### Type Renames

- [x] **TS-1**: Rename `Strategy` struct to `Scaler` across all files (exported type, cross-package -- touches 11 consumer sites in `controller/nodepool/`, 8 test struct literals, 1 compile-time interface assertion in `provider_cache.go`)
- [x] **TS-2**: Rename `drainHelper` to `nodeDrainer` (unexported, package-internal to `internal/scaling/`)
- [x] **TS-3**: Rename `drainConfig` to `drainOptions` (unexported, package-internal to `internal/scaling/`)

### Struct Field Type Change

- [x] **TS-4**: Replace `ScalingDemand.Metadata interface{}` with `Pods []corev1.Pod` (eliminates 1 type assertion site, compiler-verified)

### File Renames

- [x] **FR-01**: Rename `kubernetes.go` to `scaler.go` and `kubernetes_test.go` to `scaler_test.go` (file follows primary type)
- [x] **FR-02**: Rename `events.go` to `pod_events.go` (file contains pod event handling, follows subject_role.go convention)

### Pre-work

- [x] **PRE-01**: Commit 15 unstaged modified files on current branch before starting refactoring (overlap with rename targets)

## Out of Scope

| Item | Reason |
|------|--------|
| Rename `types.go` to `scaling_types.go` | Violates Go/K8s community convention -- `types.go` is the universal name |
| Rename `cloudprovider/types.go` to `instance_types.go` | Same convention violation, plus name collision with `aws/instance_types.go` |
| Rename `scaling` package itself | Already correct after v1.1 relocation |
| Rename `ScalingDemand`, `ScaleCalculator`, `ScaleDownCandidate` | Already follow correct Go conventions |
| Merge `drain.go` and `drain_eviction.go` | Split mirrors Karpenter pattern, aids readability |
| Remove other dead code (8 functions found by deadcode) | Out of scope -- FakeResolver methods, `IsKnownInstanceType`, `RecordStartingNodes` may be intentionally retained |

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| PRE-01 | Phase 12 | Complete |
| TS-5 | Phase 13 | Complete |
| TS-1 | Phase 14 | Complete |
| TS-2 | Phase 14 | Complete |
| TS-3 | Phase 14 | Complete |
| FR-01 | Phase 15 | Complete |
| FR-02 | Phase 15 | Complete |
| TS-4 | Phase 16 | Complete |

**Coverage:**
- Total requirements: 8
- Mapped to phases: 8/8
- Orphaned: 0

---

## Milestone Summary

**Shipped:** 8 of 8 requirements
**Adjusted:** None
**Dropped:** None

---
*Archived: 2026-02-04 as part of v1.1.1 milestone completion*

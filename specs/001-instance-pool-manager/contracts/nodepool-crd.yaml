# NodePool CRD Contract
# API Version: stratos.sh/v1alpha1
# Generated: 2026-01-19

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: nodepools.stratos.sh
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
spec:
  group: stratos.sh
  names:
    kind: NodePool
    listKind: NodePoolList
    plural: nodepools
    singular: nodepool
    shortNames:
      - np
      - npool
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: PoolSize
          type: integer
          jsonPath: .spec.poolSize
        - name: MinStandby
          type: integer
          jsonPath: .spec.minStandby
        - name: Standby
          type: integer
          jsonPath: .status.standby
        - name: Running
          type: integer
          jsonPath: .status.running
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          type: object
          description: NodePool is the Schema for the nodepools API
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              description: NodePoolSpec defines the desired state of NodePool
              required:
                - poolSize
                - minStandby
                - template
              properties:
                poolSize:
                  type: integer
                  format: int32
                  minimum: 1
                  maximum: 1000
                  description: Maximum total nodes (standby + running, excluding warmup)
                minStandby:
                  type: integer
                  format: int32
                  minimum: 0
                  description: Minimum number of nodes to maintain in stopped/standby state
                template:
                  type: object
                  description: Node template configuration
                  required:
                    - cloudProvider
                  properties:
                    labels:
                      type: object
                      additionalProperties:
                        type: string
                      description: Labels to apply to nodes
                    taints:
                      type: array
                      items:
                        type: object
                        required:
                          - key
                          - effect
                        properties:
                          key:
                            type: string
                          value:
                            type: string
                          effect:
                            type: string
                            enum:
                              - NoSchedule
                              - PreferNoSchedule
                              - NoExecute
                      description: Taints to apply to nodes
                    cloudProvider:
                      type: object
                      description: Cloud provider configuration
                      required:
                        - provider
                      properties:
                        provider:
                          type: string
                          enum:
                            - aws
                            - gcp
                            - azure
                          description: Cloud provider type
                        aws:
                          type: object
                          description: AWS EC2 configuration
                          required:
                            - instanceType
                            - ami
                            - subnetIds
                            - securityGroupIds
                            - iamInstanceProfile
                          properties:
                            instanceType:
                              type: string
                              description: EC2 instance type (e.g., "m5.large")
                            ami:
                              type: string
                              description: Amazon Machine Image ID
                            subnetIds:
                              type: array
                              minItems: 1
                              items:
                                type: string
                              description: Subnets to launch instances in
                            securityGroupIds:
                              type: array
                              minItems: 1
                              items:
                                type: string
                              description: Security groups for instances
                            iamInstanceProfile:
                              type: string
                              description: IAM instance profile ARN
                            userData:
                              type: string
                              description: Base64-encoded user data script
                            blockDeviceMappings:
                              type: array
                              items:
                                type: object
                                required:
                                  - deviceName
                                  - volumeSize
                                  - volumeType
                                properties:
                                  deviceName:
                                    type: string
                                  volumeSize:
                                    type: integer
                                    format: int32
                                  volumeType:
                                    type: string
                                  encrypted:
                                    type: boolean
                              description: EBS volume configuration
                            tags:
                              type: object
                              additionalProperties:
                                type: string
                              description: Tags to apply to instances
                scaleDown:
                  type: object
                  description: Scale-down configuration
                  properties:
                    enabled:
                      type: boolean
                      default: true
                      description: Enable automatic scale-down
                    emptyNodeTTL:
                      type: string
                      default: "5m"
                      description: Duration node must be empty before scale-down
                    drainTimeout:
                      type: string
                      default: "5m"
                      description: Maximum time to wait for node drain
                preWarm:
                  type: object
                  description: Pre-warming configuration
                  properties:
                    timeout:
                      type: string
                      default: "10m"
                      description: Time to wait for instance to self-stop
                    timeoutAction:
                      type: string
                      enum:
                        - stop
                        - terminate
                      default: stop
                      description: Action when instance doesn't self-stop
                maxNodeRuntime:
                  type: string
                  description: Maximum runtime before automatic recycling (e.g., "168h")
                reconciliationInterval:
                  type: string
                  default: "30s"
                  description: Pool maintenance loop interval
            status:
              type: object
              description: NodePoolStatus defines the observed state of NodePool
              properties:
                conditions:
                  type: array
                  items:
                    type: object
                    required:
                      - type
                      - status
                      - lastTransitionTime
                      - reason
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - "Unknown"
                      observedGeneration:
                        type: integer
                        format: int64
                      lastTransitionTime:
                        type: string
                        format: date-time
                      reason:
                        type: string
                      message:
                        type: string
                  description: Current conditions of the NodePool
                observedGeneration:
                  type: integer
                  format: int64
                  description: Last observed generation
                warmup:
                  type: integer
                  format: int32
                  description: Nodes currently initializing
                standby:
                  type: integer
                  format: int32
                  description: Nodes in stopped/standby state
                running:
                  type: integer
                  format: int32
                  description: Nodes actively running pods
                total:
                  type: integer
                  format: int32
                  description: Total node count
                lastReconcileTime:
                  type: string
                  format: date-time
                  description: Last reconciliation timestamp
